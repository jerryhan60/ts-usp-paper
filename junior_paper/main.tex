\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath, amsthm, amssymb, amsfonts}
\usepackage{mathtools}
\usepackage{bm}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{microtype}
\usepackage{enumitem}
\usepackage{natbib}
\usepackage{xcolor}
\usepackage{hyperref}
\hypersetup{colorlinks=true, linkcolor=blue!50!black, citecolor=blue!50!black, urlcolor=blue!50!black}

% Theorem environments
\theoremstyle{plain}
\newtheorem{theorem}{Theorem}[section]
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{corollary}[theorem]{Corollary}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{assumption}[theorem]{Assumption}
\theoremstyle{remark}
\newtheorem{remark}[theorem]{Remark}
\newtheorem{example}[theorem]{Example}

% Shortcuts
\newcommand{\R}{\mathbb{R}}
\newcommand{\Z}{\mathbb{Z}}
\newcommand{\N}{\mathbb{N}}
\newcommand{\C}{\mathbb{C}}
\newcommand{\E}{\mathbb{E}}
\newcommand{\Var}{\mathrm{Var}}
\newcommand{\norm}[1]{\left\lVert#1\right\rVert}
\newcommand{\abs}[1]{\left\lvert#1\right\rvert}
\newcommand{\inner}[2]{\left\langle#1,#2\right\rangle}
\DeclareMathOperator*{\argmin}{arg\,min}
\DeclareMathOperator*{\argmax}{arg\,max}
\DeclareMathOperator{\trace}{tr}
\DeclareMathOperator{\spec}{spec}
\newcommand{\method}{polynomial input preconditioning}
\newcommand{\Method}{Polynomial input preconditioning}
\newcommand{\HD}{\textsf{d4\_dropout}}
\newcommand{\Dnodrop}{\textsf{d4}}
\newcommand{\Baseline}{\textsf{Baseline}}
\newcommand{\Zero}{\textsf{Zero}}
\newcommand{\Dup}{\textsf{Duplicate}}

\title{\textbf{Polynomial Input Preconditioning for\\Universal Time Series Forecasting}}
\author{
  Jerry Han \\[0.5em]
  \small Department of Mathematics \\
  \small Princeton University \\[0.5em]
  \small Advisor: Prof.\ Elad Hazan \\
  \small Second Reader: Prof.\ Amit Singer
}
\date{Spring 2026}

\begin{document}
\maketitle

\begin{abstract}
\noindent
Patch-based transformer architectures for universal time series forecasting segment the input into fixed-length patches and embed each patch independently, creating a \emph{cross-patch information bottleneck}: temporal patterns spanning multiple patches must be recovered entirely through attention. Drawing on the recent theoretical framework of universal sequence preconditioning~\citep{marsden2024universal}, which shows that polynomial transformations of a sequence reduce the spectral complexity faced by online predictors of linear dynamical systems, we propose \textbf{\method} for patch-based transformers. We apply a fixed Chebyshev polynomial FIR filter to the raw time series and concatenate the result as an auxiliary input channel, injecting cross-patch temporal information with negligible parameter overhead ($12\,$K parameters, $0.11\%$ of the model). Two lines of justification underpin the choice of Chebyshev polynomials: (i) the classical equioscillation theorem of Chebyshev, which characterises the monic degree-$d$ polynomial of minimal supremum norm on $[-1,1]$; and (ii) the regret-reduction theorems of Marsden and Hazan, which show that such preconditioners yield sublinear, hidden-dimension-independent regret bounds for marginally stable linear dynamical systems. We analyse our specific filter's frequency response in closed form, prove a stability lemma, and validate the theoretical predictions on synthetic linear dynamical system trajectories and on the GIFT-Eval benchmark of 97 dataset--horizon configurations. Across 5 seeds and 485 paired comparisons, our method improves geometric mean MASE by $2.9\%$ over the Moirai-2 Small baseline ($p < 10^{-15}$), winning 329 of 485 comparisons after Holm--Bonferroni correction at $\alpha=0.05$. Controlled capacity experiments confirm that the improvement is driven by polynomial content rather than extra parameters: duplicating the input as a second channel \emph{hurts} performance ($44.7\%$ win rate), while a zero channel is essentially neutral. At $100$K training steps, the improvement grows to $3.9\%$. We report Holm--Bonferroni-corrected $p$-values, bootstrap confidence intervals, and an analysis of which dataset frequency-structure characteristics predict the size of the improvement, testing the spectral hypothesis implied by the theory.
\end{abstract}

\section*{Acknowledgments and Contributions}

I am deeply grateful to my advisor, Prof.\ Elad Hazan, for suggesting the connection between universal sequence preconditioning and patch-based time series foundation models, and for numerous conversations that shaped the direction of this work. I thank Prof.\ Amit Singer for serving as second reader and for feedback on the mathematical exposition. Any errors are my own.

\paragraph{Individual contributions.}
The theoretical formulation, experimental design, implementation, and writing presented in this paper are my individual work. The framework of universal sequence preconditioning as applied to online prediction of linear dynamical systems is due to \citet{marsden2024universal}; the extension of that framework to patch-based transformer forecasting, the specific filter design, the empirical methodology, the statistical analysis, and all figures and tables were developed by me. Computational experiments were run on the Princeton Research Computing cluster.

\paragraph{Relation to workshop submission.}
An abridged 4-page version of the empirical portion of this work was submitted concurrently to the ICML 2026 Workshop on Foundation Models for Structured Data (FMSD); the workshop is non-archival. This Junior Paper contains substantially expanded mathematical analysis, additional theoretical results, and additional empirical validation (synthetic linear dynamical system experiments and a spectral-correlate analysis) not present in the workshop submission.

\paragraph{Honor Code Declaration.}
This paper represents my own work in accordance with University regulations.\\[0.5em]
\textit{/s/ Jerry Han}

\tableofcontents

%==============================================================================
\section{Introduction}
%==============================================================================

Universal time series forecasting~\citep{woo2024unified, liu2024moirai2, das2024decoder, ansari2024chronos, goswami2024moment, auer2025tirex} has emerged as a pretraining paradigm for transformer-based models trained on heterogeneous corpora that span frequencies from sub-hourly to yearly, and whose behaviour at inference time is expected to transfer across unseen domains without task-specific fine-tuning. A growing body of models---Moirai, TimesFM, Chronos, MOMENT, TiRex---shares a common architectural primitive with PatchTST~\citep{nie2023patchtst} and Vision Transformer~\citep{dosovitskiy2021image}: the input sequence is segmented into non-overlapping patches of length $P$, each patch is projected to a $d$-dimensional embedding independently, and interactions across patches are mediated entirely by self-attention. This design yields substantial gains in throughput relative to token-level transformers, but it introduces what we call the \emph{cross-patch information bottleneck}: each patch embedding, before the first attention layer, encodes only $P$ time steps, so temporal correlations whose natural scale exceeds $P$ (e.g., daily periodicities in $15$-minute data, spanning $96$ steps or six patches at $P=16$) must be discovered end-to-end by the attention mechanism. Smaller models, with fewer attention heads and layers, are especially constrained.

\paragraph{Our intervention.}
We propose \textbf{\method{}}, a simple architectural change that alleviates the cross-patch bottleneck at constant parameter cost. Given the input sequence $x[t]$, we compute a \emph{preconditioning channel} by applying a fixed finite-impulse-response filter
\begin{equation}
    h[t] = (p(B_s) x)[t] = \sum_{k=0}^{d} a_k \, x[t - k s],
    \label{eq:precond-intro}
\end{equation}
where $B_s$ is the shift-by-$s$ operator $(B_s x)[t] = x[t-s]$, the polynomial $p(z) = \sum_{k=0}^d a_k z^k$ is a degree-$d$ polynomial with coefficients derived from the monic Chebyshev polynomial $\tilde{T}_d$, and the stride $s$ is set equal to the patch size $P$. The preconditioning channel is concatenated with the target and observation-mask channels before the initial per-patch projection. Only the width of this first projection grows; the transformer encoder is unchanged. For $d = 4$ and $s = P = 16$, the preconditioning channel reduces to
\[
    h[t] = x[t] - x[t - 2 P] + \tfrac{1}{8} x[t - 4 P],
\]
which injects information from two and four patches in the past into each current-patch embedding \emph{before} the first attention layer---at a cost of $12$K extra parameters, or $0.11\%$ of the Moirai-2 Small model.

\paragraph{Mathematical motivation.}
Two threads of mathematics motivate this design. The first is classical: the \emph{Chebyshev equioscillation theorem} states that among all monic polynomials of degree $d$ on the interval $[-1,1]$, the monic Chebyshev polynomial $\tilde{T}_d$ uniquely attains the minimal supremum norm, which equals $2^{1 - d}$~\citep{davis1975interpolation}. Consequently, any degree-$d$ transformation of a sequence whose power is spectrally concentrated on a normalized frequency interval is best compressed---in the uniform-norm sense---by Chebyshev coefficients. The second thread is modern: \citet{marsden2024universal} show that, within the framework of online prediction of linear dynamical systems (LDS), applying a polynomial $p(B)$ to the target sequence is \emph{equivalent} to applying $p(A)$ to the hidden transition matrix $A$, and they prove that Chebyshev and Legendre preconditioners yield the first sublinear and hidden-dimension-independent regret bounds for systems with marginally stable, possibly asymmetric, transition matrices. While patch-based transformers are not literally online predictors of LDSs, the spectral intuition transfers: a polynomial preconditioner redistributes the signal's energy across frequency bands in a way that disentangles long-scale correlations, and Chebyshev coefficients achieve the best worst-case redistribution within the monic degree-$d$ family.

\paragraph{Empirical evidence.}
We retrain the Moirai-2 Small encoder~\citep{liu2024moirai2} (11.4M parameters, six transformer blocks, $P=16$) on the LOTSA corpus~\citep{woo2024unified} for $10$K and $100$K optimizer steps, comparing four conditions: a standard \Baseline{}, a capacity-matched \Zero{} control (auxiliary channel filled with zeros), a capacity-matched \Dup{} control (auxiliary channel = copy of input), and our method \HD{} (Chebyshev $d=4$, stride $16$, $10\%$ channel dropout). On the $97$-configuration GIFT-Eval benchmark~\citep{vijay2024gifteval}, at context length $4{,}000$ and with five random seeds, \HD{} improves geometric-mean MASE by $2.9\%$ at $10$K steps ($p<10^{-15}$, sign test on $485$ paired comparisons; Holm--Bonferroni-corrected across all evaluated methods) and by $3.9\%$ at $100$K steps. Importantly, the \Dup{} capacity control \emph{hurts} performance ($44.7\%$ win rate) and \Zero{} is neutral ($54\%$, $p=0.042$ unadjusted), identifying the polynomial content---not the extra parameters---as the source of the gain. Cross-domain analysis confirms the spectral hypothesis: gains are largest on high-frequency domains (transport, energy) whose natural time scales align with the filter's lookback structure, and negligible on weather and healthcare where the dominant variations occur at scales either much shorter or much longer than our stride-$16$ filter.

\paragraph{Contributions and Outline.}
The remainder of this paper develops the following contributions:
\begin{enumerate}[leftmargin=1.5em, topsep=0.2em, itemsep=0.2em]
  \item A rigorous statement (Section~\ref{sec:preliminaries} and Section~\ref{sec:precondition}) of the mathematical framework of universal sequence preconditioning, specialized from online LDS prediction to patch-based transformer forecasting, including the frequency-response analysis of our specific filter.
  \item A self-contained proof sketch (Section~\ref{sec:chebyshev}) of the Chebyshev equioscillation theorem and its implications for polynomial preconditioning, and a closed-form derivation of the frequency response $H(e^{j\omega})$ of our preconditioning filter (Theorem~\ref{thm:freq-response}).
  \item A stability lemma (Lemma~\ref{lem:stability}) establishing that the preconditioning operation preserves bounded variance and is hence BIBO-stable, relevant for practical numerical behavior.
  \item An empirical validation (Section~\ref{sec:empirical}) on: (a) synthetic marginally-stable linear dynamical systems, where the predicted spectral attenuation $\abs{H(\omega_k)}^2 = 2^{2-2d}$ matches the empirical PSD notch depth quantitatively, while a regret-side probe with a well-specified offline AR forecaster shows---as expected from the limits of the regret-reduction theorems---that preconditioning helps only when the forecaster has limited capacity to capture multi-patch correlations on its own; (b) the GIFT-Eval benchmark, where polynomial input preconditioning improves a capacity-limited patch-based transformer by $2.9\%$ MASE; this is supplemented by extensive capacity-control ablations and a spectral-correlate analysis that tests the frequency-response hypothesis dataset-by-dataset.
  \item A rigorous statistical methodology section (Section~\ref{sec:stats}) presenting multiple-testing-corrected significance tests, bootstrap confidence intervals, and effect sizes, supporting the paper's inferential claims and contextualizing the size of the observed improvement.
\end{enumerate}

%==============================================================================
\section{Preliminaries}
\label{sec:preliminaries}
%==============================================================================

We review two strands of background that our development relies on: linear dynamical systems and their forecasting, and Chebyshev approximation theory. The patch-based transformer architecture and the specific Moirai-2 instantiation we use are deferred to Section~\ref{sec:patch-transformers}. A reader familiar with any of these may skim the corresponding subsection.

\subsection{Linear Dynamical Systems and Sequence Forecasting}

A \emph{linear dynamical system} (LDS) with hidden dimension $m$ and observation dimension $n$ is specified by matrices $A \in \R^{m \times m}$, $C \in \R^{n \times m}$, and sequences of process noise $w_t \in \R^m$ and observation noise $e_t \in \R^n$. The system evolves according to
\begin{equation}
    h_{t+1} = A h_t + w_t, \qquad y_t = C h_t + e_t, \qquad t \in \Z_{\geq 0},
    \label{eq:lds}
\end{equation}
with initial state $h_0 \in \R^m$. In the \emph{online prediction} problem, a learner sees $y_1, y_2, \ldots$ sequentially and at each step $t$ outputs a prediction $\hat y_{t+1}$ of the next observation; performance is measured by the cumulative regret
\[
    \mathrm{Regret}_T = \sum_{t=1}^T \ell(\hat y_{t+1}, y_{t+1}) - \min_{\text{class}} \sum_{t=1}^T \ell(\tilde y_{t+1}, y_{t+1}),
\]
where $\ell$ is a convex loss (typically squared error) and the minimum ranges over a comparator class such as linear functions of $y_{1:t}$.

The \emph{spectral radius} $\rho(A) = \max_i |\lambda_i(A)|$ of the transition matrix controls the forecastability of the system. When $\rho(A) < 1$ (strictly stable), the memory of the system decays geometrically and classical regret bounds of order $O(\sqrt{T} \, \mathrm{poly}(m))$ are available~\citep{hazan2017learning}. When $\rho(A) = 1$ (marginally stable, e.g., pure sinusoids or integrated processes), standard bounds degrade, with the dependence on $m$ often becoming linear or worse.

\subsection{Chebyshev Polynomials}
\label{sec:chebyshev-prelim}

The \emph{Chebyshev polynomials of the first kind}, denoted $T_d(x)$ for $d \in \Z_{\geq 0}$, are defined by the recurrence
\begin{equation}
    T_0(x) = 1, \quad T_1(x) = x, \quad T_{d+1}(x) = 2 x \, T_d(x) - T_{d-1}(x),
    \label{eq:cheb-recurrence}
\end{equation}
or equivalently by the trigonometric identity $T_d(\cos \theta) = \cos(d \theta)$. The first several polynomials are $T_2(x) = 2x^2 - 1$, $T_3(x) = 4x^3 - 3x$, $T_4(x) = 8x^4 - 8x^2 + 1$. The leading coefficient of $T_d$ is $2^{d-1}$, so the \emph{monic Chebyshev polynomial} is $\tilde T_d(x) = T_d(x) / 2^{d-1}$. For $d = 4$,
\[
    \tilde T_4(x) = x^4 - x^2 + \tfrac{1}{8}.
\]

Chebyshev polynomials possess several extremal properties; one of the most celebrated is the \emph{equioscillation property} underlying the approximation theorem we state in Section~\ref{sec:chebyshev}.

%==============================================================================
\section{Universal Sequence Preconditioning}
\label{sec:precondition}
%==============================================================================

We now review the framework of \citet{marsden2024universal}, which motivates our filter design, and specialize it to patch-based forecasting.

\subsection{Polynomial Preconditioning of LDS Observations}

Consider an LDS as in~\eqref{eq:lds} with noise terms neglected for exposition. Then $y_t = C A^t h_0$ and the $d$-step differenced or preconditioned observation
\[
    \tilde y_t \;=\; (p(B) y)[t] \;=\; \sum_{k=0}^d a_k y_{t-k} \;=\; C \Big( \sum_{k=0}^d a_k A^{-k} \Big) A^t h_0 \;=\; C \, p(A^{-1}) A^t h_0,
\]
where $B$ denotes the backshift operator and $p(z) = \sum_{k=0}^d a_k z^k$. Reversing the polynomial (equivalently, working with the reciprocal polynomial $p^*(z) = z^d p(1/z)$) gives the cleaner identity
\[
    \tilde y_t \;=\; C \, p^*(A) A^{t-d} h_0,
\]
so that preconditioning in observation space corresponds to \emph{multiplying the hidden state's dynamics by $p^*(A)$}. If the eigenvalues of $A$ lie in an interval $[-\rho, \rho] \subset \R$ (real spectrum), then for any monic degree-$d$ polynomial $q$,
\[
    \rho\big( q(A) \big) \leq \max_{\lambda \in [-\rho, \rho]} \abs{ q(\lambda) },
\]
and the right-hand side is \emph{minimized over monic $q$ of degree $d$} by $q = \rho^d \, \tilde T_d(\,\cdot\,/\rho)$---this is the content of the classical Chebyshev minimax theorem (Theorem~\ref{thm:chebyshev-minimax} below). Preconditioning by the Chebyshev-derived polynomial therefore compresses the effective spectral radius seen by a downstream online predictor, yielding the regret-reduction theorems of~\citet[Thms.~3 and~5]{marsden2024universal}.

\subsection{The Marsden--Hazan Regret-Reduction Framework}
\label{sec:mh-regret}

The qualitative argument that ``smaller $\rho(p^*(A))$ means easier prediction'' is made precise by~\citet{marsden2024universal}. Their results require a careful treatment of the spectral residual that we will not reproduce in full; instead we restate the underlying \emph{algebraic mechanism} (an exact identity that holds noise-free) and the \emph{informal heuristic} that emerges from it. The reader interested in the exact regret rates---which depend on the specific online learner, the marginal-stability assumption, and a careful summation of the spectral residual over time---is referred to~\citep[Theorems~3 and~5]{marsden2024universal}.

Fix an LDS as in~\eqref{eq:lds} with hidden dimension $m$, transition matrix $A$ with $\rho(A) \leq 1$, and observation matrix $C \in \R^{1 \times m}$. Let $p$ be a monic polynomial of degree $d$ with coefficient vector $\boldsymbol{a} = (a_0, \ldots, a_d)$ (with $a_0 = 1$), and consider preconditioning the observation stream by $\tilde y_t := (p(B) y)[t] = \sum_{k=0}^{d} a_k\, y_{t-k}$.

\begin{lemma}[Algebraic identity for preconditioned observations; cf.\ {\citet[Lemma~2]{marsden2024universal}}]
\label{lem:mh-algebra}
In the noise-free case ($w_t \equiv 0$, $e_t \equiv 0$), with $h_0$ the LDS initial state,
\begin{equation}
    \tilde y_t \;=\; \sum_{k=0}^{d} a_k \, C A^{t-k} h_0 \;=\; C \cdot p^*(A) \cdot A^{t-d} \, h_0,
    \label{eq:mh-identity}
\end{equation}
where $p^*(z) := z^d p(1/z) = \sum_{k=0}^{d} a_k z^{d-k}$ is the reciprocal polynomial of $p$. With process and observation noise included, $\tilde y_t = C \cdot p^*(A) \cdot A^{t-d} h_0 + \tilde w_t + \tilde e_t$, where $\tilde e_t = (p(B) e)[t]$ is a moving-average of the observation noise with $\trace(\E[\tilde e_t \tilde e_t^\top]) = \norm{\boldsymbol{a}}_2^2 \cdot \trace(\Sigma_e)$, and $\tilde w_t$ is the analogous filtered process noise.
\end{lemma}

\begin{proof}
Direct calculation: $\tilde y_t = \sum_k a_k C A^{t-k} h_0 = C \big(\sum_k a_k A^{d-k}\big) A^{t-d} h_0$, and $\sum_k a_k A^{d-k}$ is exactly $p^*(A)$ by definition of $p^*$. The noise calculation is by linearity and isotropy of the noise covariance.\qedhere
\end{proof}

Lemma~\ref{lem:mh-algebra} is the load-bearing identity: $p$ is applied to the \emph{output} sequence \emph{once} (not iterated), and its effect is to replace the implicit dependence on $A^{t-d}$ by $p^*(A) \cdot A^{t-d}$. Because $A$ has spectrum in $[-\rho(A), \rho(A)]$, $p^*(A)$ has spectrum given by $\{p^*(\lambda) : \lambda \in \spec(A)\}$, and the contraction
\begin{equation}
    \norm{p^*(A) \cdot A^{t-d} \, h_0}_2 \;\leq\; \rho(p^*(A)) \cdot \rho(A)^{t-d} \cdot \norm{h_0}_2
    \label{eq:mh-contraction}
\end{equation}
shrinks the relevant LDS magnitude by a factor of $\rho(p^*(A))$ uniformly in $t$. We summarise the heuristic implication.

\begin{quote}\itshape
\textbf{Heuristic regret reduction.} If a downstream online forecaster is run on the preconditioned sequence $\tilde y$, its cumulative squared-error regret splits qualitatively into three parts: (i) an \emph{algorithm} term (e.g., $O(L \log T)$ for online gradient descent on a $L$-dimensional comparator), (ii) a \emph{spectral residual} term that scales with $\rho(p^*(A))^2$ summed over time, and (iii) a \emph{noise-gain} term proportional to $\norm{\boldsymbol{a}}_2^2 \cdot \trace(\Sigma_e)$ that grows with the filter's $\ell^2$ coefficient norm.
\end{quote}

\noindent The exact form of the spectral residual is delicate: when $\rho(A) < 1$, the geometric series $\sum_t \rho(A)^{2(t-d)}$ converges and the spectral term contributes $O(1)$; when $\rho(A) = 1$ (true marginal stability), the series diverges and~\citet{marsden2024universal} use a more careful argument to bound it by something sublinear in $T$. We do not reproduce that argument; for our purposes, the operative observation is that \emph{whatever bound holds for the spectral residual, it is monotone in $\rho(p^*(A))^2$, which is in turn minimized by the Chebyshev choice of $p$} (Theorem~\ref{thm:chebyshev-minimax} below). With $p(\lambda) = \rho^d \tilde T_d(\lambda/\rho)$ for $\rho = \rho(A)$, the contraction satisfies
\begin{equation}
    \rho\big(p^*(A)\big) \;\leq\; \sup_{\lambda \in [-\rho, \rho]} \abs{p^*(\lambda)} \;=\; \sup_{\lambda \in [-\rho, \rho]} \abs{p(\lambda)} \;\leq\; \rho^d \cdot 2^{1-d},
    \label{eq:mh-spectral-bound}
\end{equation}
where the second equality uses that $p$ and $p^*$ have the same coefficient set up to reversal and hence the same supremum norm on $[-\rho, \rho]$.

The dichotomy in the heuristic above is the source of the spectral--noise tradeoff: increasing $d$ shrinks the spectral residual (geometrically, by~\eqref{eq:mh-spectral-bound}) at the cost of a growing noise-gain term $\norm{\boldsymbol{a}}_2^2$. Section~\ref{sec:noise-gain} computes the latter exactly. The cited Marsden--Hazan regret theorems then give specific, dimension-independent regret rates as $T \to \infty$ under stated assumptions on the comparator class and the marginal-stability of $A$; their precise form is not used in what follows. What we \emph{do} use is the qualitative shape of the heuristic and the structural fact that the spectral term is minimized by Chebyshev preconditioning.

\subsection{Specialization to Patch-based Forecasting}

Our setting differs from the LDS online-prediction setting in three ways: (i) the forecaster is a pretrained transformer, not an online least-squares or follow-the-regularized-leader algorithm; (ii) the data-generating process is unknown and not assumed to be a finite-dimensional LDS; (iii) rather than \emph{replacing} the target sequence by its preconditioned version, we \emph{concatenate} the preconditioned sequence as an auxiliary input channel.

Design (iii) is key. Replacing $y$ by $p(B) y$ discards the (low-frequency or unshaped) content of the original signal, which the transformer may still use; concatenation hands the model both representations and lets attention layers combine them. It also preserves the forecasting target: we precondition the \emph{input}, not the supervision signal.

Despite these differences, the spectral intuition transfers. A preconditioning filter redistributes the input's energy across frequency bands; Chebyshev coefficients achieve the minimax-optimal redistribution for a given filter degree; and a patch-based transformer facing a signal whose spectrum has been shaped to highlight multi-patch correlations is plausibly better-equipped to capture them.

%==============================================================================
\section{Theoretical Analysis}
\label{sec:chebyshev}
%==============================================================================

This section establishes the load-bearing analytic results: the classical Chebyshev minimax theorem (Theorem~\ref{thm:chebyshev-minimax}), a closed-form frequency response for our filter (Theorem~\ref{thm:freq-response}), a stability lemma for the preconditioning operation (Lemma~\ref{lem:stability}), and a noise-gain analysis (Lemma~\ref{lem:noise-gain}).

\paragraph{Notation table.} Several distinct objects appear repeatedly in this section; we collect them here for reference.
\begin{center}
\small
\begin{tabular}{ll}
\toprule
\textbf{Symbol} & \textbf{Meaning} \\
\midrule
$T_d(x)$ & classical Chebyshev polynomial of degree $d$, leading coefficient $2^{d-1}$ for $d \geq 1$ \\
$\tilde T_d(x) = T_d(x) / 2^{d-1}$ & monic Chebyshev polynomial; the minimax minimiser of Theorem~\ref{thm:chebyshev-minimax} \\
$\tilde T_d^*(z) = z^d \tilde T_d(1/z)$ & reciprocal monic Chebyshev; the polynomial actually applied as the FIR filter \\
$p(z) = \tilde T_d^*(z)$ & the FIR polynomial: $p(z) = 1 - z^2 + z^4/8$ for $d=4$ \\
$h[t] = (p(B_s) x)[t]$ & full-filter output: $\sum_k a_k x[t-ks]$ at stride $s$ \\
$\tilde h[t] = (p(B_s) x)[t] - x[t]$ & \emph{residual} preconditioning channel; this is what the model receives \\
$H(\omega) = p(e^{-j 2\pi s \omega})$ & DTFT of the full filter: spectrum on the unit circle \\
$\widetilde H(\omega) = H(\omega) - 1$ & DTFT of the residual filter \\
$\boldsymbol{a}^{(d)}$ & coefficient vector of $\tilde T_d$ in the monomial basis \\
\bottomrule
\end{tabular}
\end{center}
The architectural input projection sees $\tilde h$, not $h$; analyses involving the model's input distribution use $\widetilde H$. Analyses concerning the LDS algebraic identity (Section~\ref{sec:mh-regret}) use $p$ and $p^*$; the spectral-radius bound goes through $\sup_{[-\rho,\rho]} \abs{p}$, which equals $\sup_{[-\rho,\rho]} \abs{p^*}$ since $p$ and $p^*$ have the same coefficient set up to reversal.

\subsection{Chebyshev Minimax and the Equioscillation Theorem}

\begin{theorem}[Chebyshev Minimax; cf.\ \citealp{davis1975interpolation}]
\label{thm:chebyshev-minimax}
Among all real monic polynomials of degree $d \geq 1$, the monic Chebyshev polynomial $\tilde T_d$ uniquely minimizes $\sup_{x \in [-1,1]} \abs{q(x)}$:
\begin{equation}
    \tilde T_d \;=\; \argmin_{q \text{ monic, } \deg q = d} \; \max_{x \in [-1,1]} \abs{q(x)}, \qquad \max_{x \in [-1,1]} \abs{\tilde T_d(x)} = 2^{1-d}.
\end{equation}
\end{theorem}

\begin{proof}
We give the full argument in three steps: (1) compute the supremum norm of $\tilde T_d$, (2) identify the equioscillation points, (3) prove uniqueness.

\emph{Step 1: $\sup_{[-1,1]} \abs{\tilde T_d} = 2^{1-d}$.} By the trigonometric definition $T_d(\cos\theta) = \cos(d\theta)$, the substitution $x = \cos\theta$ for $\theta \in [0, \pi]$ traces $T_d$ exactly over $[-1,1]$, with $T_d(\cos\theta) = \cos(d\theta)$ ranging in $[-1, 1]$. Therefore $\sup_{[-1,1]} \abs{T_d} = 1$, and dividing by the leading coefficient $2^{d-1}$ gives $\sup_{[-1,1]} \abs{\tilde T_d} = 2^{1-d}$.

\emph{Step 2: equioscillation points.} The function $\theta \mapsto \cos(d\theta)$ achieves its extrema $\pm 1$ at the $d+1$ angles $\theta_k = k\pi/d$ for $k = 0, 1, \ldots, d$, alternating in sign: $\cos(d\theta_k) = (-1)^k$. Translating back via $x_k = \cos(\theta_k) = \cos(k\pi/d)$, we obtain $d+1$ points in $[-1,1]$ at which $\tilde T_d(x_k) = (-1)^k \cdot 2^{1-d}$. The points are arranged in decreasing order: $1 = x_0 > x_1 > \cdots > x_d = -1$.

\emph{Step 3: uniqueness via the equioscillation argument.} Suppose for contradiction that $q$ is a monic polynomial of degree $d$ with
\[
    M := \sup_{x \in [-1,1]} \abs{q(x)} < 2^{1-d}.
\]
Define $r := \tilde T_d - q$. Since both $\tilde T_d$ and $q$ are monic of degree $d$, the leading terms cancel and $\deg r \leq d - 1$. Evaluate $r$ at the equioscillation points:
\[
    r(x_k) \;=\; \tilde T_d(x_k) - q(x_k) \;=\; (-1)^k \cdot 2^{1-d} - q(x_k).
\]
Since $\abs{q(x_k)} \leq M < 2^{1-d}$, the term $(-1)^k \cdot 2^{1-d}$ dominates the right-hand side in magnitude, so $\mathrm{sign}\big(r(x_k)\big) = (-1)^k$ for each $k = 0, 1, \ldots, d$. The sequence $r(x_0), r(x_1), \ldots, r(x_d)$ therefore alternates in sign, exhibiting $d$ sign changes between consecutive entries. By the intermediate-value theorem (applied $d$ times to the continuous function $r$ on the intervals $[x_{k+1}, x_k]$), $r$ has at least $d$ zeros in $(-1, 1)$. But a non-zero polynomial of degree $\leq d-1$ has at most $d-1$ zeros. Hence $r \equiv 0$, i.e., $q = \tilde T_d$. This is a contradiction with $M < 2^{1-d}$, so no such $q$ exists.

The argument also shows uniqueness of the minimizer: if any $q^*$ achieves $\sup \abs{q^*} = 2^{1-d}$ but is not $\tilde T_d$, then $r = \tilde T_d - q^*$ has the property that $\mathrm{sign}(r(x_k))$ matches $\mathrm{sign}(\tilde T_d(x_k))$ at each $x_k$ where $\abs{q^*(x_k)} < 2^{1-d}$. A standard variant of the argument (using strict inequality on a non-empty subset) yields $r$ with at least $d$ zeros in $[-1,1]$, again forcing $r \equiv 0$.\qedhere
\end{proof}

An immediate corollary is the worst-case spectral-compression statement used in Section~\ref{sec:precondition}:

\begin{corollary}[Worst-case spectral compression]
\label{cor:spectral-compression}
Let $\mathcal{S}_\rho := \{A \in \R^{m \times m} : A \text{ diagonalizable with real spectrum in } [-\rho, \rho]\}$. Among all monic polynomials $q$ of degree $d$, the choice $q(\lambda) = \rho^d \, \tilde T_d(\lambda / \rho)$ minimizes the worst-case spectral radius $\sup_{A \in \mathcal{S}_\rho} \rho(q(A))$, attaining the bound
\[
    \sup_{A \in \mathcal{S}_\rho} \rho\big( q(A) \big) \;=\; \sup_{\lambda \in [-\rho, \rho]} \abs{q(\lambda)} \;\leq\; \rho^d \cdot 2^{1 - d}.
\]
\end{corollary}

\begin{remark}
For a \emph{fixed} matrix $A$ with a known eigenvalue set $\Lambda_A = \{\lambda_1, \ldots, \lambda_m\} \subset [-\rho, \rho]$, the Chebyshev choice is in general not optimal: a monic polynomial of degree $d \geq m$ that vanishes at every $\lambda_i$ achieves $\rho(q(A)) = 0$. Corollary~\ref{cor:spectral-compression} is a minimax statement over the worst-case spectrum in $[-\rho, \rho]$, which is the relevant setting for a forecaster that does not know $A$ in advance and must perform uniformly across the family $\mathcal{S}_\rho$.
\end{remark}

Corollary~\ref{cor:spectral-compression} captures the worst-case spectral-shrinkage benefit motivating polynomial preconditioning: by minimax-optimally compressing the supremum norm of $q$ on $[-\rho, \rho]$, the Chebyshev choice gives the strongest \emph{universal} (spectrum-agnostic) guarantee on the contraction $\rho(p^*(A))$ that drives the spectral-residual term in the regret heuristic of Section~\ref{sec:mh-regret}.

\subsection{Frequency Response of the Preconditioning Filter}

Our preconditioning filter is defined by $h[t] = (p(B_s) x)[t]$ with $p(z) = \sum_{k=0}^d a_k z^k$, where $B_s$ denotes the shift-by-$s$ operator and the coefficients $(a_0, \ldots, a_d)$ are chosen such that $a_0 = 1$ and $(a_1, \ldots, a_d)$ are the lower-order monic-Chebyshev coefficients in reverse order. Explicitly for $d = 4$: $(a_0, a_1, a_2, a_3, a_4) = (1, 0, -1, 0, 1/8)$, so $p(z) = 1 - z^2 + z^4 / 8$. We call this polynomial the \emph{reciprocal monic Chebyshev} $\tilde T_d^*(z) := z^d \tilde T_d(1/z)$; it agrees with $\tilde T_d$ up to a change of variable and scales identically.

\begin{theorem}[Frequency Response and Attenuation Values]
\label{thm:freq-response}
Let $p(z) = \tilde T_d^*(z) = z^d \tilde T_d(1/z)$, and let the DTFT of the filter applied at stride $s$ be
\[
    H(\omega) \;:=\; p\big( e^{-j 2 \pi s \omega} \big) \;=\; \sum_{k=0}^{d} a_k \, e^{-j 2 \pi k s \omega},
\]
with $\omega \in [-1/2, 1/2]$ in cycles per sample. Then:
\begin{enumerate}[leftmargin=1.5em, itemsep=0.2em]
    \item $\abs{H(\omega)}^2$ is a real trigonometric polynomial in $\cos(2 \pi s \omega)$ of degree $d$, periodic in $\omega$ with period $1/s$.
    \item $H$ has \emph{no zeros} on the unit circle: the real roots of $\tilde T_d$ lie strictly in $(-1, 1)$, and their reciprocals (roots of $\tilde T_d^*$) lie strictly outside, hence neither set meets $\{\abs{z}=1\}$.
    \item For even $d$, the magnitude of $H$ at $\omega_k := k/(2s)$, $k \in \Z$, equals
    \begin{equation}
        \abs{H(\omega_k)} \;=\; \abs{p(1)} \;=\; \abs{p(-1)} \;=\; 2^{\,1-d},
        \label{eq:min-magnitude}
    \end{equation}
    matching the Chebyshev minimax bound of Theorem~\ref{thm:chebyshev-minimax}. These points are critical points of $\abs{H(\omega)}^2$.
\end{enumerate}
\end{theorem}

\begin{proof}
Part~(i): expanding $\abs{H(\omega)}^2 = H(\omega) \overline{H(\omega)}$ and grouping like frequencies via $e^{jk\phi} e^{-j\ell\phi} = e^{j(k-\ell)\phi}$ with $\phi = 2\pi s \omega$, every surviving term has the form $a_k a_\ell \cos((k-\ell)\phi)$. Using the Chebyshev identity $\cos(n\phi) = T_n(\cos\phi)$ inductively, $\abs{H(\omega)}^2$ is a polynomial in $\cos\phi$ of degree at most $d$; this degree is attained by the $a_0 a_d \cos(d\phi)$ cross-term. Periodicity in $\omega$ with period $1/s$ follows from $e^{-j 2\pi k s (\omega + 1/s)} = e^{-j 2\pi k s \omega}$.

Part~(ii): the roots of $\tilde T_d$ are $\cos((2j-1)\pi/(2d))$ for $j = 1,\ldots,d$, all real and strictly inside $(-1,1)$. Under the transform $z \mapsto 1/z$, real numbers in $(-1, 1) \setminus \{0\}$ map to real numbers with $\abs{z} > 1$, so the roots of $\tilde T_d^*$ lie strictly outside the unit disk. Neither set meets the unit circle, hence $H(\omega) = \tilde T_d^*(e^{-j 2 \pi s \omega}) \neq 0$ for all $\omega \in \R$.

Part~(iii): for even $d$, the odd-degree coefficients of $\tilde T_d$ vanish, so $p(1) = p(-1) = \sum_k a_k = T_d(1)/2^{d-1} = 1/2^{d-1} = 2^{1-d}$. At $\omega_k = k/(2s)$ we have $e^{-j 2\pi s \omega_k} = e^{-j\pi k} = (-1)^k$, so $H(\omega_k) = p((-1)^k) = p(\pm 1)$. Differentiating $\abs{H(\omega)}^2$ expressed as a polynomial in $u = \cos(2\pi s \omega)$, we obtain $\frac{d}{d\omega}\abs{H}^2 = -2\pi s \sin(2\pi s \omega) \cdot Q'(u)$ for the polynomial $Q$ of part~(i); the prefactor $\sin(2\pi s \omega)$ vanishes at $\omega = \omega_k$, so these are critical points.\qedhere
\end{proof}

\begin{remark}
We do \emph{not} claim the points $\omega_k$ are global minima of $\abs{H}^2$ in general; the proof only establishes that they are critical points and that $\abs{H(\omega_k)} = 2^{1-d}$. For our main experimental settings $d \in \{2, 4, 6\}$ we verify by direct computation that $\abs{H(\omega)}^2$, viewed as a polynomial $Q(u)$ in $u = \cos(2\pi s \omega) \in [-1, 1]$, is convex with vertex outside $[-1, 1]$, so its minimum on $[-1, 1]$ is attained at $u = 1$, i.e., at $\omega = \omega_k$. For $d = 4$ explicitly: $Q(u) = u^2/2 - (9/4)u + 113/64$, with vertex $u^* = 9/4 > 1$, so $Q$ is strictly decreasing on $[-1, 1]$ and attains its minimum value $1/64$ at $u = 1$.
\end{remark}

Theorem~\ref{thm:freq-response}(iii) has a clean interpretation: at the frequencies $\omega = 0, 1/(2s), 1/s, \ldots$, the filter attenuates by a factor of exactly $2^{1-d}$---the Chebyshev minimax constant. This matches the worst-case spectral-compression statement of Corollary~\ref{cor:spectral-compression} and is verified empirically in Section~\ref{sec:synth-lds} (Figure~\ref{fig:synth-spectrum}).

\begin{example}[$d = 4$, $s = 16$: filter attenuation frequencies]
The attenuation minima for our main experimental setting lie at $\omega \in \{0, 1/32, 1/16, 3/32, \ldots\}$ cycles per sample, with $\abs{H(\omega_k)} = 1/8$. On $15$-minute sampled data, $\omega = 1/32$ corresponds to a period of $32 \times 15\,\text{min} = 8\,\text{h}$ (\emph{not} a daily period). The relationship between the filter's attenuation frequencies and dataset-level spectral content is examined in Section~\ref{sec:spectral-correlate}; we note here that the filter's lookback lags ($2s, 4s$ time steps) and its attenuation frequencies are distinct mathematical objects and need not coincide with dataset-level periodicities.
\end{example}

\subsection{Stability of the Preconditioning Operation}

\begin{lemma}[BIBO Stability and Variance Preservation]
\label{lem:stability}
Let $x$ be a real-valued sequence with $\sup_t \abs{x[t]} \leq M$ and $\Var(x[t]) \leq \sigma^2$, and let $h[t] = (\tilde T_d^*(B_s) x)[t]$ for $d \geq 1$ and $s \geq 1$. Then:
\begin{enumerate}[leftmargin=1.5em, itemsep=0.1em]
    \item (BIBO stability) $\sup_t \abs{h[t]} \leq C_d \cdot M$ where $C_d = \sum_{k=0}^{d} \abs{a_k}$ is the $\ell^1$ norm of the filter coefficients.
    \item (Variance bound) If $x$ is wide-sense stationary, $\Var(h[t]) \leq \big(\sum_k a_k^2\big) \, \sigma^2 + 2 \sum_{k \neq \ell} \abs{a_k a_\ell} \sigma^2 = \big(\sum_k \abs{a_k}\big)^2 \sigma^2 = C_d^2 \sigma^2$.
\end{enumerate}
For $d = 4$, $C_4 = 1 + 1 + 1/8 = 2.125$, giving $\sup_t \abs{h[t]} \leq 2.125 M$ and $\Var(h[t]) \leq 4.52 \sigma^2$.
\end{lemma}

\begin{proof}
Both bounds are immediate from the triangle inequality applied to $h[t] = \sum_k a_k x[t-ks]$.\qedhere
\end{proof}

Lemma~\ref{lem:stability} ensures that the preconditioning channel does not cause pathological numerical behaviour. In practice we find the empirical variance ratio $\Var(h[t]) / \Var(x[t])$ to be much smaller than the upper bound $C_d^2$ for realistic time series, because of non-adversarial correlation structure.

\subsection{Noise Gain and the Spectral--Noise Tradeoff}
\label{sec:noise-gain}

The heuristic regret reduction of Section~\ref{sec:mh-regret} contains two competing terms whose dependence on the filter degree $d$ runs in opposite directions: a \emph{spectral residual} that scales with $\rho(p^*(A))^2$ and shrinks as $4^{1-d}$, and a \emph{noise-gain} term $\norm{\boldsymbol{a}}_2^2 \trace(\Sigma_e)$ that grows with the filter's $\ell^2$ coefficient norm. We make the noise-gain side of this tradeoff quantitative.

\begin{lemma}[Noise Gain of Chebyshev Preconditioning]
\label{lem:noise-gain}
Let $\boldsymbol{a}^{(d)} = (a_0^{(d)}, \ldots, a_d^{(d)})$ denote the coefficient vector of the monic Chebyshev polynomial $\tilde T_d$ written in the monomial basis $\{1, x, \ldots, x^d\}$. Then
\[
    \norm{\boldsymbol{a}^{(d)}}_2^2 \;=\; 1,\; 1,\; \tfrac{5}{4},\; \tfrac{25}{16},\; \tfrac{129}{64},\; \tfrac{681}{256},\; \tfrac{3653}{1024},\; \tfrac{19825}{4096},\; \tfrac{108545}{16384}, \qquad d = 0, 1, 2, 3, 4, 5, 6, 7, 8.
\]
The squared-norm grows exponentially in $d$: the consecutive ratios $\norm{\boldsymbol{a}^{(d+1)}}_2^2 / \norm{\boldsymbol{a}^{(d)}}_2^2$ approach $((1+\sqrt 2)/2)^2 = (3 + 2\sqrt 2)/4 \approx 1.457$ as $d \to \infty$. The spectral-compression squared norm $(2^{1-d})^2 = 4^{1-d}$ from Theorem~\ref{thm:chebyshev-minimax} decays at the faster rate $1/4$ per unit increment in $d$, so the ratio of spectral compression to noise gain decays as $(1.457/4)^d \approx 0.364^d$, exponentially in $d$.
\end{lemma}

\begin{proof}
The exact values follow from the Chebyshev recurrence~\eqref{eq:cheb-recurrence}: monomial-basis coefficients of $\tilde T_d = T_d / 2^{d-1}$ are obtained by expanding the recurrence; both the values listed above and the consecutive ratios are easily verified by direct computation. The asymptotic ratio statement is a numerical observation that we have verified up to $d = 20$; we omit a closed-form asymptotic for $\norm{\boldsymbol{a}^{(d)}}_2^2$, since the precise constant is not needed for any later argument---only the qualitative fact that $\norm{\boldsymbol{a}^{(d)}}_2^2$ grows at a rate strictly slower than $4^d$ (so that the spectral term still dominates the noise term for $d$ in the working range).
\end{proof}

\begin{remark}[Heuristic optimal degree]
\label{rem:noise-tradeoff}
The structure of the heuristic regret bound (Section~\ref{sec:mh-regret}) is qualitatively
\[
    \text{regret per unit time} \;\approx\; A \cdot 4^{-d} \;+\; B \cdot \norm{\boldsymbol{a}^{(d)}}_2^2,
\]
where $A$ aggregates the LDS contraction constants and $B$ aggregates the noise-covariance constants. Treating $\norm{\boldsymbol{a}^{(d)}}_2^2 \approx C \cdot \alpha^d$ with $\alpha = (3+2\sqrt 2)/4$, the optimal degree obeys $4^{-d^*} \cdot \log 4 \cdot A \approx \alpha^{d^*} \cdot \log\alpha \cdot B C$, giving
\[
    d^* \;\approx\; \tfrac{1}{\log(4 \alpha)}\,\log\!\big(A / (BC)\big),
\]
which depends only on the ratio of LDS-contraction to noise-covariance constants and is \emph{independent of the time horizon $T$}. We treat this as a back-of-the-envelope explanation for why a small fixed degree (in our experiments, $d=4$) suffices: for fixed $A/B$, no asymptotically growing $d^*$ is needed. We do not derive a regret-rate corollary from the heuristic; for that we again refer to~\citet{marsden2024universal}.
\end{remark}

For our application: at $d = 4$, the squared-spectral residual decays by a factor of $4^{1-4} = 1/64$ relative to no preconditioning, while the noise gain is $\norm{\boldsymbol{a}^{(4)}}_2^2 = 129/64 \approx 2.02$. The signal-to-noise leverage ratio is therefore $64 / 2.02 \approx 31.7$, which justifies $d = 4$ for low-noise regimes. At $d = 8$ the ratio improves to $4^7 / (108545/16384) \approx 2475$, but the spectral-compression term is already deeply saturated relative to the noise gain by $d = 4$. Empirically (Table~\ref{tab:ablations-main}(a)), $d \in \{3, 4, 5, 6, 8\}$ are statistically indistinguishable in MASE, consistent with the spectral term being deeply saturated by $d \geq 3$ on patch-based foundation models.

\subsection{The Residual Filter $p(B) - 1$}
\label{sec:residual-filter}

Our architecture concatenates the \emph{residual} preconditioning channel $\tilde h[t] := (p(B_s) x)[t] - x[t] = ((p - 1)(B_s) x)[t]$, not the raw filter output, since the original channel $x[t]$ is already provided to the input projection. We characterize the residual filter's frequency response.

\begin{proposition}[Residual Filter Frequency Response]
\label{prop:residual-filter}
Let $\tilde p(z) := p(z) - 1$ for $p = \tilde T_d^*$. The DTFT of the stride-$s$ residual filter $\tilde p(B_s)$ is
\[
    \widetilde H(\omega) \;=\; H(\omega) - 1 \;=\; \sum_{k=1}^{d} a_k \, e^{-j 2\pi k s \omega}.
\]
At the attenuation minima $\omega_k = k/(2s)$ from Theorem~\ref{thm:freq-response},
\[
    \abs{\widetilde H(\omega_k)} \;=\; \abs{H(\omega_k) - 1} \;=\; \abs{2^{1-d} - 1}.
\]
For $d = 4$, $\abs{\widetilde H(\omega_k)} = \abs{1/8 - 1} = 7/8$, while the residual filter \emph{passes} (gain $\geq 1$) the frequencies where $H$ has gain $\geq 2$.
\end{proposition}

\begin{proof}
Linearity of the DTFT gives $\widetilde H = H - 1$. Substituting the minimum value $H(\omega_k) = 2^{1-d}$ from Theorem~\ref{thm:freq-response}(iii) yields the stated magnitude.\qedhere
\end{proof}

The residual filter is therefore a bandpass-like operator: it passes (or amplifies) frequencies that the raw filter $p$ amplifies, and attenuates frequencies that $p$ attenuates---but neither perfectly nulls nor perfectly preserves any frequency. This is the desired behavior: the channel injects \emph{novel} information about cross-patch correlations that complements the raw signal already available in the target channel.

\subsection{Comparison with Other Polynomial Bases}
\label{sec:basis-comparison}

The minimax property of Theorem~\ref{thm:chebyshev-minimax} singles out Chebyshev among the classical orthogonal polynomial families. Different orthogonality weights yield different optimization criteria, and these criteria interact with the structure of the regret heuristic of Section~\ref{sec:mh-regret} in a quantifiable way. We summarise the key alternatives and prove the relevant comparison.

\paragraph{Setup.} On $[-1, 1]$, the classical orthogonal polynomial families $\{T_d\}, \{U_d\}, \{P_d\}, \{P_d^{(\alpha,\beta)}\}$ are characterized by their orthogonality weights:
\begin{center}
\small
\begin{tabular}{lll}
\toprule
\textbf{Family} & \textbf{Weight $w(x)$ on $[-1,1]$} & \textbf{Optimal norm} \\
\midrule
Chebyshev (1st kind) $T_d$ & $1/\sqrt{1-x^2}$ & $L^\infty$ (uniform) \\
Chebyshev (2nd kind) $U_d$ & $\sqrt{1-x^2}$ & weighted $L^2$ \\
Legendre $P_d$ & $1$ (uniform) & $L^2$ \\
Jacobi $P_d^{(\alpha,\beta)}$ & $(1-x)^\alpha (1+x)^\beta$ & weighted $L^2$ \\
\bottomrule
\end{tabular}
\end{center}
On $\R$, Hermite polynomials are orthogonal w.r.t.\ $e^{-x^2}$, which is the wrong space here: the eigenvalues of marginally stable real LDSs are bounded in $[-1,1]$, not spread over all of $\R$. We therefore restrict attention to families on $[-1,1]$.

The Marsden--Hazan regret heuristic (Section~\ref{sec:mh-regret}) contains the residual term $\rho(p^*(A))^2 \leq \big(\sup_{[-1,1]} \abs{q(\lambda)}\big)^2$ for monic $q$ of degree $d$ matching the polynomial structure (with $A$'s spectrum in $[-1,1]$). For diagonalizable $A$ with real spectrum in $[-1,1]$, the relevant norm is $L^\infty$ (uniform), \emph{not} $L^2$.

\begin{theorem}[Legendre Minimax in $L^2$]
\label{thm:legendre-minimax}
The monic Legendre polynomial $\tilde P_d$ is the unique minimizer of $\norm{q}_{L^2[-1,1]} = \big(\int_{-1}^1 q(x)^2 \, dx\big)^{1/2}$ over real monic polynomials $q$ of degree $d$. Its $L^2$ norm is
\[
    \norm{\tilde P_d}_{L^2}^2 \;=\; \frac{2^{2d+1} (d!)^4}{(2d+1) ((2d)!)^2}.
\]
\end{theorem}

\begin{proof}
The orthogonality $\int_{-1}^1 P_n(x) P_m(x) dx = \frac{2}{2n+1} \delta_{nm}$ implies that the projection of any monic polynomial $q$ of degree $d$ onto $\mathrm{span}\{P_0, \ldots, P_{d-1}\}$ contributes a non-negative term to $\norm{q}_{L^2}^2$ which is zero only when the projection vanishes, i.e., when $q$ is a scalar multiple of $P_d$. Among such multiples, the unique monic one is $\tilde P_d = P_d / c_d$ where $c_d = (2d)! / (2^d (d!)^2)$ is the leading coefficient. The norm follows from $\norm{P_d}_{L^2}^2 = 2/(2d+1)$.\qedhere
\end{proof}

\begin{proposition}[Sup-Norm Gap Between Chebyshev and Legendre]
\label{prop:cheb-vs-legendre}
For $d \geq 1$,
\[
    \frac{\sup_{[-1,1]} \abs{\tilde T_d}}{\sup_{[-1,1]} \abs{\tilde P_d}} \;=\; \frac{2^{1-d}}{2^d (d!)^2 / (2d)!} \;=\; \frac{(2d)!}{2^{2d-1} (d!)^2} \;=\; \frac{2 \binom{2d}{d}}{4^{d}}.
\]
This ratio equals $1$ at $d = 1$ and is strictly less than $1$ for all $d \geq 2$, with explicit values
\[
    d = 1, 2, 3, 4, 5, 6, 7, 8 \;\;\Longrightarrow\;\; \text{ratio} = 1,\; \tfrac{3}{4},\; \tfrac{5}{8},\; \tfrac{35}{64},\; \tfrac{63}{128},\; \tfrac{231}{512},\; \tfrac{429}{1024},\; \tfrac{6435}{16384}.
\]
Asymptotically, the ratio decays as $2/\sqrt{\pi d}$, so the Chebyshev sup norm is smaller than the Legendre sup norm by a factor of order $\sqrt{\pi d}/2$.
\end{proposition}

\begin{proof}
The Chebyshev sup norm $2^{1-d}$ is from Theorem~\ref{thm:chebyshev-minimax}. For Legendre: since $\abs{P_d(x)} \leq 1$ on $[-1,1]$ with equality at $x = \pm 1$, the monic Legendre $\tilde P_d = P_d / c_d$ has sup norm $1/c_d = 2^d (d!)^2 / (2d)!$. The ratio is then $2^{1-d} \cdot (2d)! / (2^d (d!)^2) = (2d)! / (2^{2d-1} (d!)^2) = 2 \binom{2d}{d} / 4^d$. By Stirling, $\binom{2d}{d} \sim 4^d / \sqrt{\pi d}$, so the ratio is asymptotic to $2/\sqrt{\pi d}$. At $d = 1$, $\binom{2}{1} = 2$ and $4^1 = 4$, giving ratio $2 \cdot 2 / 4 = 1$ exactly (Chebyshev and Legendre coincide at degree $1$: $\tilde T_1(x) = \tilde P_1(x) = x$).\qedhere
\end{proof}

The proposition has a sharp implication: at $d = 4$, the monic Chebyshev attenuates a worst-case eigenvalue by a factor of $1/8 = 0.125$, while monic Legendre attenuates it by only $8/35 \approx 0.229$---roughly $1.83\times$ less compression. Since the spectral-residual term in the regret heuristic squares this quantity, the Chebyshev residual term is approximately $1.83^2 \approx 3.35\times$ smaller than the Legendre residual term at the same degree.

\paragraph{Empirical confirmation.} The basis ablation (Appendix~\ref{app:ablations}, Table~\ref{tab:basis-ablation}) is consistent with the theory:
\begin{center}
\small
\begin{tabular}{lcc}
\toprule
Basis ($d{=}4$, $s{=}16$) & MASE & $\Delta\%$ vs.\ Baseline \\
\midrule
Chebyshev $\tilde T_4^*$ & $0.8234$ & $-4.99\%$ \\
EMA (truncated geometric) & $0.8329$ & $-3.89\%$ \\
Legendre $\tilde P_4^*$ & $0.8400$ & $-3.07\%$ \\
Finite differences & $0.8569$ & $-1.12\%$ \\
\bottomrule
\end{tabular}
\end{center}
Chebyshev outperforms Legendre by $1.92\%$ relative MASE---of similar order of magnitude to the theoretical sup-norm-squared gap of $3.35\times$ in the residual term, after accounting for the fact that the residual is one of two competing terms in the regret bound (the other, noise gain, also differs between bases).

\paragraph{Chebyshev second kind.} The polynomials $U_d$ satisfy $\int_{-1}^1 U_d U_m \sqrt{1-x^2}\, dx = (\pi/2) \delta_{dm}$, optimizing a weighted $L^2$ norm rather than $L^\infty$. Since $U_d$ has leading coefficient $2^d$ and $\sup_{[-1,1]} \abs{U_d} = U_d(1) = d+1$ \citep[Eq.\ 1.41]{mason2003chebyshev}, the monic $\tilde U_d = U_d / 2^d$ has sup norm $(d+1)/2^d$, strictly larger than $\tilde T_d$'s $2^{1-d} = 2 / 2^d$ for $d \geq 2$. Under the heuristic of Section~\ref{sec:mh-regret}, $U_d$-based preconditioning would yield a spectral-residual term inflated by a factor of $\big((d+1)/2\big)^2$ relative to Chebyshev first-kind; we did not run a $U_d$ ablation, so this is a prediction rather than an empirical finding.

\paragraph{Why not learn the coefficients?} A natural alternative is to drop the fixed Chebyshev structure and let the model learn the FIR coefficients from Gaussian initialization. Empirically (Appendix~\ref{app:ablations}, Table~\ref{tab:learned-fir}), learnable coefficients converge to a high-pass-like solution that beats baseline by $-3.85\%$ but is statistically worse than fixed Chebyshev ($-4.99\%$). Fixing the polynomial provides an inductive bias---an architectural prior that the model would otherwise have to discover from data. This is consistent with the broader theme that classical analytic structure (orthogonal polynomials, Plancherel duality, equioscillation) is more sample-efficient than free parameter optimisation when the structure is correct.

\subsection{Informal Regret-Style Bound in a Toy Setting}

To give intuition for \emph{why} preconditioning might help forecasting, consider a simplified scalar online prediction problem: $x_t = \sum_{i=1}^m \alpha_i \lambda_i^t + \varepsilon_t$ with $\varepsilon_t$ sub-Gaussian, $\lambda_i \in [-1, 1]$ real, and a linear learner predicting $\hat x_{t+1} = \beta^\top x_{(t-L):t}$ over a window of length $L$. The cumulative square-error regret of such a learner against the best-in-hindsight linear predictor scales with the condition number of the design Gram matrix $G = \E[x_{(t-L):t} x_{(t-L):t}^\top]$. Preconditioning by $\tilde T_d^*$ replaces each $\lambda_i^t$ by $\tilde T_d(\lambda_i) \lambda_i^{t-d}$, attenuating modes near the boundary of $[-1,1]$ by a factor of at most $2^{1-d}$ and thus reducing $\mathrm{cond}(G)$. Making this precise requires additional care, and we defer a formal regret statement to future work. We caution that the toy intuition above does \emph{not} predict an MSE reduction for offline AR forecasters that already capture the dominant dynamics: in Section~\ref{sec:synth-lds} we confirm empirically that a well-specified AR$(L=3)$ predictor, fit by least squares on a synthetic LDS trajectory, has \emph{higher} test MSE under preconditioning, because the filter's variance amplification ($\norm{\boldsymbol{a}}_2^2 \geq 1$) outweighs its spectral-attenuation benefit for that forecaster class. The benefit of preconditioning manifests in regimes the toy bound is silent on---in particular, capacity-limited pretrained transformers facing many tasks at once.

%==============================================================================
\section{Patch-based Transformers for Forecasting}
\label{sec:patch-transformers}
%==============================================================================

Our experiments are conducted on the \emph{Moirai-2 Small} time series foundation model~\citep{liu2024moirai2}, the open-source\footnote{Available on the HuggingFace Hub as \texttt{Salesforce/moirai-2.0-R-small}; the architecture and training recipe follow the description in \citet{liu2024moirai2}.} dense, decoder-only successor to the original Moirai encoder~\citep{woo2024unified}. We chose Moirai-2 because it is a small ($11.4$M parameter), publicly reproducible, state-of-the-art universal forecaster: small enough to train from scratch on a single H100 within wall-clock budgets compatible with five-seed ablations, yet competitive with much larger models on the GIFT-Eval benchmark. Our preconditioning method modifies only the input projection of this model; understanding the surrounding architecture, training objective, training corpus, and evaluation protocol is therefore essential context for the empirical results in Section~\ref{sec:empirical}. We summarise each in turn.

\subsection{Patches and the Per-Patch Projection}
\label{sec:patches}

A patch-based transformer forecaster~\citep{nie2023patchtst, woo2024unified, liu2024moirai2} proceeds as follows. Given a univariate history $x[1{:}T]$, partition it into non-overlapping patches $\mathbf{p}_i = (x[(i{-}1)P{+}1], \ldots, x[iP])$ of length $P$ for $i=1, \ldots, T/P$. A per-patch projection (in Moirai-2, a residual block of two linear layers with a SiLU nonlinearity) maps each patch to an embedding $\mathbf{e}_i \in \R^{d_{\text{model}}}$. These embeddings are fed to a transformer block stack whose output is decoded into a predictive distribution over future patches via a per-patch projection head. Two features of this architecture are salient for our development:
\begin{itemize}[leftmargin=1.5em, itemsep=0.1em, topsep=0.2em]
    \item \emph{Cross-patch information must be learned via attention.} Before the first self-attention operation, $\mathbf{e}_i$ is a function of $\mathbf{p}_i$ alone; it contains no information about $\mathbf{p}_{i-1}$, $\mathbf{p}_{i+1}$, or more distant patches. Patterns whose natural temporal scale exceeds $P$ time steps (e.g., daily periodicities in $15$-minute data, spanning $96$ steps or $6$ patches at $P=16$) must be discovered end-to-end by the attention layers above the projection.
    \item \emph{The per-patch projection is narrow.} For Moirai-2 Small ($P=16$, $d_{\text{model}}=384$), the projection's input dimension is $2P = 32$ (target plus observation mask), so it has roughly $32 \cdot 384 \approx 12{,}000$ parameters out of $11.4$M total ($\sim 0.1\%$). Adding a third $P$-dimensional input channel costs an additional $P \cdot d_{\text{model}} = 6{,}144$ parameters; altering downstream layers, in contrast, is much more expensive.
\end{itemize}
These two observations motivate \emph{input-side} preconditioning: a fixed FIR filter applied before patching can inject cross-patch information into each $\mathbf{e}_i$ at negligible parameter cost, leaving the rest of the network untouched.

\subsection{The Moirai-2 Small Architecture}
\label{sec:moirai-arch}

Moirai-2 Small is a decoder-only transformer with $L = 6$ layers, hidden dimension $d_{\text{model}} = 384$, feed-forward dimension $d_{\text{ff}} = 1024$, $h = 6$ attention heads, and patch size $P = 16$. Each transformer block applies pre-norm self-attention with rotary position embeddings (RoPE)~\citep{su2024rope} and a SwiGLU feed-forward network~\citep{shazeer2020glu}, with RMSNorm~\citep{zhang2019rms} replacing LayerNorm throughout. A binary attention bias~\citep{liu2024moirai2} discriminates between positions in the observed prefix and positions in the prediction horizon. Inputs are $z$-scored at the sequence level before patching using a packed-sequence variant of standard $z$-score scaling. Three features of the architecture are particularly relevant to the rest of this paper:
\begin{itemize}[leftmargin=1.5em, itemsep=0.1em, topsep=0.2em]
    \item \emph{Decoder-only with causal mask.} Unlike the masked-language-model encoder used in Moirai 1.0~\citep{woo2024unified}, Moirai-2 attends to past patches only. Predictions for patch $i$ depend on $\mathbf{e}_1, \ldots, \mathbf{e}_i$ but not on later patches.
    \item \emph{Multi-token prediction head.} The output projection emits, for each input patch, predictions for the next $K = 4$ patches at $9$ quantile levels $\{0.1, 0.2, \ldots, 0.9\}$, yielding $K \cdot 9 \cdot P = 576$ output values per patch.
    \item \emph{Per-patch input residual block.} The input projection is a two-layer residual MLP $\mathrm{ResBlock}_{2P \to d_{\text{model}}}$ acting on the concatenation $[\mathbf{p}_i; \mathbf{m}_i]$ of patch and observation mask. This is the only layer that touches raw patch values; it is also the only layer modified by our method (Section~\ref{sec:method}).
\end{itemize}

\subsection{Pretraining Objective and Optimization}
\label{sec:pretraining-objective}

Each training sample is a packed sequence of patched univariate series. A random \emph{prefix ratio} (default $0.3$) determines how much of the sequence is treated as observed context; the remainder is the prediction window. Within the prediction window, the target patches are replaced by a learnable mask token, and the model is trained to predict their values. Additionally, with probability $\texttt{mask\_ratio} = 0.5$, individual patches inside the prediction window are corrupted to encourage robustness to missing observations.

The training loss is the \emph{packed quantile MAE loss},
\begin{equation}
    \mathcal{L} \;=\; \frac{1}{N \cdot |\mathcal{Q}|} \sum_{n,t,q} \rho_q\!\left(\, y_{n,t} - \hat y_{n,t}^{(q)} \,\right),
    \label{eq:quantile-loss}
\end{equation}
where the sum runs over patches in the prediction window of all packed samples, $\mathcal{Q} = \{0.1, \ldots, 0.9\}$ is the set of quantile levels, $\hat y_{n,t}^{(q)}$ is the model's $q$-quantile prediction, and $\rho_q(u) = u(q - \mathbf{1}[u < 0])$ is the standard pinball loss~\citep{koenker1978regression}. We use AdamW~\citep{loshchilov2019adamw} with $\beta_1 = 0.9$, $\beta_2 = 0.98$, $\epsilon = 10^{-6}$, weight decay $0.1$, peak learning rate $\eta = 10^{-3}$, $1{,}000$-step linear warmup, and cosine annealing to zero. Training is performed in bf16 mixed precision with batch size $256$ on a single H100 GPU. Anomaly filtering~\citep{liu2024moirai2} resamples training windows whose normalised values exceed a $z$-score threshold of $8.0$ to suppress extreme outliers in LOTSA. The full set of hyperparameters appears in Appendix~\ref{app:methodology}.

\subsection{Training Corpus: LOTSA}
\label{sec:lotsa}

Moirai-2 is pretrained on the \emph{Large-scale Open Time Series Archive} (LOTSA), introduced by \citet{woo2024unified}, which aggregates $\sim 27$\,billion observations across $9$ broad domains: energy, transport, climate, cloud operations, web, sales, healthcare, nature, and economic/financial. Domains are unbalanced---a few datasets (e.g., Buildings-900K, ERA5) dominate raw observation counts---so Moirai-2 uses \emph{variate-proportional weighted sampling}: for each dataset, the sampling weight is inversely proportional to its raw observation count and proportional to the number of distinct variates, which roughly equalises the contribution of large multivariate datasets and small univariate ones. We use the released LOTSA v1 weighting scheme without modification (the \texttt{lotsa\_v1\_moirai2} configuration in the official codebase). Throughout training, each sample is normalised by its own mean and standard deviation (computed within the observed prefix) before patching.

\subsection{Inference and Forecasting}
\label{sec:inference}

At inference, the model is presented with the full observed history followed by mask tokens for the prediction window. Because the multi-token head emits a $K{=}4$-patch forecast for every input patch, predictions for the prediction-window patches are read directly from the corresponding output positions; no autoregressive rollout is required for horizons of up to $K \cdot P = 64$ time steps. For longer horizons, the predicted patches are appended to the context and the procedure repeats. Each forecast at horizon $h$ produces a vector of $9$ quantile estimates at each time step; for our headline metric (MASE), we use the median ($q = 0.5$) as the point forecast.

\subsection{Evaluation: GIFT-Eval}
\label{sec:gifteval}

We evaluate on \textsc{GIFT-Eval}~\citep{vijay2024gifteval}, an open benchmark of $97$ dataset $\times$ horizon configurations spanning $24$ datasets and $7$ frequency classes (sub-hourly through yearly), curated to test the zero-shot generalization claim of universal forecasters. Each configuration specifies a held-out split, prediction horizon, and patch-size convention; we follow the recommended convention of $P = 8$ for quarterly data and $P = 32$ for all other frequencies, with context length $4{,}000$. Each model's forecast is scored by the \emph{normalised MASE},
\begin{equation}
    \mathrm{MASE}^*(\text{model}, c) \;=\; \frac{\mathrm{MASE}(\text{model}, c)}{\mathrm{MASE}(\text{seasonal naive}, c)},
    \label{eq:normalised-mase}
\end{equation}
which divides each configuration's raw MASE by the seasonal-naive baseline's MASE on the same configuration. A score below $1$ means the model beats seasonal-naive on that configuration. The headline aggregate is the geometric mean of $\mathrm{MASE}^*$ across all $97$ configurations. We report cross-seed mean and standard deviation, paired sign-test counts (out of $97$ per seed; $5 \cdot 97 = 485$ pooled across $5$ seeds), and Holm--Bonferroni-corrected $p$-values; the full statistical methodology is in Section~\ref{sec:statistics}.

\subsection{Why this baseline matters for the present work}
\label{sec:why-moirai}

Three features of the Moirai-2 setup interact with our method in ways that frame the empirical results:
\begin{enumerate}[leftmargin=1.5em, itemsep=0.1em, topsep=0.2em]
    \item \emph{Universal training corpus.} Because LOTSA spans many domains and frequencies, the model must implicitly learn frequency-aware features. A fixed Chebyshev preconditioner provides such features ``for free'' at the input, which is consistent with the empirical observation (Appendix~\ref{app:domain}) that gains are largest on high-frequency domains whose dominant scales align with the filter's lookback structure.
    \item \emph{Small parameter budget.} At $11.4$M parameters, Moirai-2 Small is at the lower end of the universal forecaster scale~\citep{liu2024moirai2}. This is the regime in which a fixed inductive bias is most likely to help, since the model has less capacity to discover the relevant cross-patch correlations purely through self-attention. Section~\ref{sec:discussion} discusses why our method's gains shrink at the larger Moirai-2 Base scale.
    \item \emph{GIFT-Eval as held-out evaluation.} GIFT-Eval is held out from LOTSA, so improvements at $\mathrm{MASE}^*$ are a clean signal of better generalisation rather than overfitting to training datasets. Our $97$-configuration $\times$ $5$-seed evaluation yields $485$ paired comparisons, allowing tight bootstrap intervals and well-powered sign tests.
\end{enumerate}

%==============================================================================
\section{Method: Polynomial Input Preconditioning}
\label{sec:method}
%==============================================================================

\subsection{Architecture}

Let $x \in \R^T$ be a univariate time series that has been normalized to zero mean and unit variance. Our method modifies a standard patch-based transformer forecaster by replacing the per-patch input $(\mathbf{p}_i, \mathbf{m}_i)$---target patch and observation mask---by the augmented triple $(\mathbf{p}_i, \mathbf{m}_i, \tilde{\mathbf{h}}_i)$, where the \emph{preconditioning residual}
\[
    \tilde h[t] \;:=\; \tilde T_d^*(B_s) x[t] - x[t]
\]
is computed over the entire (normalized) series before patching and $\tilde{\mathbf{h}}_i$ is the $P$-length window of $\tilde h$ aligned with the $i$-th patch. We work with the residual rather than the full filter output so that the preconditioning channel captures the \emph{novel} information introduced by preconditioning (subtracting the raw signal, which is already available in $\mathbf{p}_i$). The per-patch projection becomes
\begin{equation}
    \mathbf{e}_i = \text{ResBlock}_{3P \to d}([\mathbf{p}_i; \mathbf{m}_i; \tilde{\mathbf{h}}_i]) \in \R^d,
\end{equation}
where $\text{ResBlock}$ consists of a widened linear layer followed by a SiLU nonlinearity, a second linear layer, and a residual connection. Only the first linear layer widens; downstream layers are unchanged.

\subsection{Channel Dropout}

During training, we zero out each patch's preconditioning channel independently with probability $p_{\text{drop}} = 0.1$, at inference providing the full preconditioning signal. This serves two purposes: (i) it prevents catastrophic dependence on the preconditioning channel (models trained without dropout degrade by $\sim 36\%$ in MASE when the channel is replaced by zeros at inference; see Appendix~\ref{app:inference-ablation}), and (ii) it has a small regularizing effect, empirically yielding a $\sim 0.3\%$ improvement over the no-dropout variant.

\subsection{Parameter Count}

For Moirai-2 Small ($P = 16$, $d_{\text{model}} = 384$, $d_{\text{ff}} = 1024$), widening the first linear layer from $32 \to 384$ (target + mask) to $48 \to 384$ (target + mask + preconditioning) adds $16 \cdot 384 = 6{,}144$ parameters; the residual path contributes another $\approx 6{,}000$. The total overhead is $\approx 12$K parameters, or $0.105\%$ of the $11.4$M baseline. No other parameters are added: the transformer encoder, per-head projection weights, output heads, and normalization layers are identical to the baseline.

%==============================================================================
\section{Empirical Results}
\label{sec:empirical}
%==============================================================================

\subsection{Experimental Setup}

Our experimental protocol exactly matches Section~\ref{sec:patch-transformers}: we train Moirai-2 Small from scratch on LOTSA v1 (with the official Moirai-2 weighting) for $10$K optimizer steps---and $100$K in Section~\ref{sec:100k}---and evaluate on GIFT-Eval at context length $4{,}000$ across all $97$ dataset $\times$ horizon configurations. The four conditions of interest below share \emph{identical} training data, optimizer, schedule, seeds, and evaluation pipeline; only the content of the auxiliary input channel differs. We run five seeds per condition, $\{0, 1, 2, 7, 42\}$, yielding $5 \times 97 = 485$ paired comparisons per contrast. All headline aggregates use the geometric-mean normalised MASE of Eq.~\eqref{eq:normalised-mase}, with cross-seed mean and standard deviation reported throughout.

\subsection{Main Results}
\label{sec:main-results}

\begin{table}[h]
\centering
\caption{Main results on GIFT-Eval, 5 seeds, 485 paired comparisons per method. $\Delta\%$ is the geometric-mean MASE change relative to \Baseline{}. Win rates are sign-test wins in paired per-configuration comparisons. Holm--Bonferroni-adjusted $p$-values are reported in the last column, correcting across the three comparisons in this table.}
\label{tab:main}
\begin{tabular}{lcccc}
\toprule
\textbf{Method} & \textbf{GeoMean MASE} & \textbf{$\Delta$\% vs \Baseline{}} & \textbf{Wins / 485} & \textbf{$p_{\text{HB}}$} \\
\midrule
\HD{} (Ours) & $\mathbf{0.837}$ & $\mathbf{-2.89\%}$ & $\mathbf{329}$ & $\mathbf{<10^{-14}}$ \\
\Zero{} (capacity control) & $0.858$ & $-0.45\%$ & $262$ & $0.13$ \\
\Baseline{} & $0.862$ & --- & --- & --- \\
\Dup{} (capacity control) & $0.865$ & $+0.38\%$ & $217$ & $>0.99$ \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:main} shows that \HD{} improves geometric-mean MASE by $2.89\%$ over \Baseline{} at $10$K steps (pooled $p_{\text{HB}} < 10^{-14}$). Notably, neither capacity control provides a comparable benefit: \Dup{} \emph{hurts} performance ($44.7\%$ wins vs.\ $50\%$ expected by chance), and \Zero{} is at most weakly beneficial. These controls are diagnostic: they isolate the \emph{polynomial content} of the preconditioning channel, as opposed to the mere presence of an additional input channel or an extra $12$K parameters, as the causal driver of the gain.

\subsection{Ablation: Filter Degree, Stride, Basis, and Dropout}

\begin{table}[h]
\centering
\small
\caption{Filter ablations on GIFT-Eval (97 configs, $10$K steps). Degree row uses 5 seeds (mean); other rows use seed 0. Best in \textbf{bold}.}
\label{tab:ablations-main}
\begin{tabular}{lccccccc}
\toprule
\multicolumn{8}{c}{\textbf{(a) Degree} (Chebyshev, $s=16$, dropout $10\%$, $5$ seeds, mean MASE)} \\
\midrule
$d$ & $2$ & $3$ & $\mathbf{4}$ & $5$ & $6$ & $7$ & $8$ \\
MASE & $0.850$ & $0.842$ & $\mathbf{0.837}$ & $0.842$ & $0.840$ & $0.847$ & $0.841$ \\
Std & $0.013$ & $0.005$ & $0.009$ & $0.008$ & $0.011$ & $0.011$ & $0.005$ \\
\midrule
\multicolumn{8}{c}{\textbf{(b) Stride} (Chebyshev $d{=}4$, dropout $10\%$, seed 0)} \\
\midrule
$s$ & $4$ & $8$ & $\mathbf{16}$ & $32$ & & & \\
MASE & $0.841$ & $0.852$ & $\mathbf{0.823}$ & $0.848$ & & & \\
\midrule
\multicolumn{8}{c}{\textbf{(c) Basis} ($d{=}4$, $s{=}16$, dropout $10\%$, seed 0)} \\
\midrule
Basis & \textbf{Cheby} & EMA & Legendre & FiniteDiff & & & \\
MASE & $\mathbf{0.823}$ & $0.833$ & $0.840$ & $0.857$ & & & \\
\midrule
\multicolumn{8}{c}{\textbf{(d) Channel Dropout} (Chebyshev $d{=}4$, $s{=}16$, seed 0)} \\
\midrule
$p_{\text{drop}}$ & $0\%$ & $2\%$ & $\mathbf{10\%}$ & $20\%$ & & & \\
MASE & $0.838$ & $0.839$ & $\mathbf{0.823}$ & $0.842$ & & & \\
\bottomrule
\end{tabular}
\end{table}

The ablations in Table~\ref{tab:ablations-main} align with theoretical predictions. (a) Across $d \in \{2, \ldots, 8\}$ at 5 seeds each, $d = 4$ has the lowest mean MASE, but the curve is shallow: $d \in \{3, 5, 6, 8\}$ are within roughly one seed-to-seed standard deviation, and only $d = 2$ (a single non-trivial lag) and $d = 7$ trail meaningfully. We treat $d=4$ as a representative working choice rather than a uniquely optimal one---the qualitative ``every $d \geq 2$ improves over the baseline'' conclusion is the seed-robust statement, and is what theory predicts. (b) Stride $s = P$ is optimal: filter taps align exactly with patch boundaries, so the preconditioning channel injects cross-patch structure at the natural scale. (c) Chebyshev beats EMA (a one-sided low-pass filter), Legendre (orthogonal but not minimax), and finite-difference (degenerate high-pass), confirming the minimax-optimality argument. (d) Dropout $10\%$ avoids both over-reliance on the channel (low dropout) and under-use (high dropout).

\subsection{Synthetic Linear Dynamical System Validation}
\label{sec:synth-lds}

To test the theoretical predictions of Theorems~\ref{thm:chebyshev-minimax}--\ref{thm:freq-response} on data with known generative structure, we simulate trajectories from a near-marginally-stable normal LDS with $m = 5$ hidden dimensions, spectral radius $\rho(A) = 0.99$ (strictly stable but close to the marginal-stability boundary), real eigenvalues placed on the interval $[-\rho, \rho]$ with random signs, and orthogonal change-of-basis $V$ (so $A$ is normal: $V^\top A V$ is diagonal). The trajectories include small additive Gaussian process/observation noise ($\sigma_w = \sigma_e = 0.02$). We apply the Chebyshev-preconditioning operator at stride $s = 3$ for degrees $d \in \{0, 2, 3, 4, 5, 6\}$, where $d = 0$ denotes no preconditioning.

\paragraph{Validation of Theorem~\ref{thm:freq-response}: spectral attenuation.}
Figure~\ref{fig:synth-spectrum} shows the power spectral density (Welch-estimated, $512$-point window) of the simulated LDS output before and after preconditioning. Consistent with Theorem~\ref{thm:freq-response}(iii), the preconditioned PSD exhibits deep notches at the predicted attenuation frequencies $\omega = 0, 1/6, 1/3$ cycles per sample (for $s=3$). The notch depth grows as predicted: at each attenuation point $\abs{H(\omega_k)}^2 = (2^{1-d})^2 = 2^{2-2d}$, yielding PSD reductions of $1/4$ for $d=2$, $1/64$ for $d=4$ (the orange curve in Figure~\ref{fig:synth-spectrum}), and $1/1024$ for $d=6$---closely matching the observed notch depths.

\begin{figure}[h]
\centering
\includegraphics[width=0.85\linewidth]{figures/synth_lds_spectrum.pdf}
\caption{Power spectral density of a near-marginally-stable normal LDS trajectory ($\rho=0.99$, $m=5$), before (black) and after (colors) Chebyshev preconditioning at stride $s=3$. Dotted lines indicate the theoretical attenuation-point frequencies $\omega = 0, 1/6, 1/3$ predicted by Theorem~\ref{thm:freq-response}. The notch depth scales with $d$ as $2^{2-2d}$, verified empirically.}
\label{fig:synth-spectrum}
\end{figure}

\paragraph{Validation of Lemma~\ref{lem:noise-gain}: noise gain of the preconditioning filter.}
The Marsden--Hazan regret bound contains two competing terms (Section~\ref{sec:noise-gain}): a spectral residual that decays as $4^{1-d}$ and a noise gain $T \cdot \norm{\boldsymbol{a}^{(d)}}_2^2 \cdot \trace(\Sigma_e)$ that grows with $d$. Lemma~\ref{lem:noise-gain} fixes the exact noise-gain norms: $\norm{\boldsymbol{a}^{(0)}}_2^2 = 1$, $\norm{\boldsymbol{a}^{(2)}}_2^2 = 5/4$, $\norm{\boldsymbol{a}^{(4)}}_2^2 = 129/64 \approx 2.02$. To test these predictions in isolation, we deliberately put preconditioning in the regime where the spectral-residual term is \emph{not} the bottleneck, by running a forecaster that is already well-specified for the LDS dynamics: an offline batch-fit AR($L=3$) predictor.

\begin{figure}[h]
\centering
\includegraphics[width=0.85\linewidth]{figures/synth_lds_regret.pdf}
\caption{Test MSE of an offline AR($L=3$) forecaster on a near-marginally-stable normal LDS trajectory ($\rho=0.99$, $m=5$, stride $s=3$, averaged over 15 random LDS realisations and the test tail) versus training prefix length, for raw and Chebyshev-preconditioned input at degrees $d \in \{0, 2, 4\}$. Markers indicate the empirical MSE; the ordering $\text{MSE}(d{=}0) < \text{MSE}(d{=}2) < \text{MSE}(d{=}4)$ matches the prediction of Lemma~\ref{lem:noise-gain} that test MSE in this regime is dominated by the noise-gain norm $\norm{\boldsymbol{a}^{(d)}}_2^2$.}
\label{fig:synth-regret}
\end{figure}

The empirical MSE ratios, averaged over the 7 training-prefix lengths, are
\begin{equation*}
    \frac{\overline{\mathrm{MSE}}(d{=}0)}{\overline{\mathrm{MSE}}(d{=}2)} \;=\; 0.872, \qquad
    \frac{\overline{\mathrm{MSE}}(d{=}0)}{\overline{\mathrm{MSE}}(d{=}4)} \;=\; 0.603.
\end{equation*}
Lemma~\ref{lem:noise-gain} predicts, in a regime where the spectral-residual term is small, that test MSE should approximately scale with $\norm{\boldsymbol{a}^{(d)}}_2^2$, giving (under that simplification) reference ratios $1/(5/4) = 0.800$ and $1/(129/64) = 0.496$. The observed ratios are larger than the pure-noise-gain reference but smaller than $1$---qualitatively consistent with the heuristic of Section~\ref{sec:mh-regret}, in which preconditioning provides a small spectral benefit and a larger noise penalty for a forecaster already well-specified for the data-generating process. We avoid quantitatively decomposing the gap into ``spectral'' and ``noise'' contributions (the cleanly separated decomposition would require a controlled-noise variant of the experiment); the ordering itself, $\text{MSE}(d{=}0) < \text{MSE}(d{=}2) < \text{MSE}(d{=}4)$, is the conclusion we draw.

Why does the same filter help a pretrained transformer (Section~\ref{sec:main-results}, $-2.9\%$ MASE) but \emph{hurt} an offline AR model? Two structural differences: (i) the transformer is not tuned to the specific data-generating process of any single GIFT-Eval task and is the wrong size for many of them ($11.4$M parameters), so its effective spectral-residual term is far from saturated and the spectral benefit dominates the noise-gain cost; (ii) cross-patch attention from a random initialisation has limited capacity to recover multi-patch correlations, exactly the capacity limit the preconditioning channel alleviates. Figure~\ref{fig:synth-regret} is therefore both a quantitative test of Lemma~\ref{lem:noise-gain}---the noise-gain norms predict the empirical MSE ordering---and a clean demonstration that polynomial input preconditioning is a \emph{regime-dependent} intervention: helpful only where the spectral-residual term is the bottleneck.

\subsection{Spectral-Correlate Analysis on GIFT-Eval}
\label{sec:spectral-correlate}

To test the spectral mechanism at the dataset level, we ask whether datasets whose spectral mass aligns with frequencies the filter \emph{preserves} benefit more than datasets whose mass aligns with frequencies the filter \emph{attenuates}. Since the model receives the residual channel $\tilde h[t] = (p(B_s)x - x)[t]$ rather than the raw filter output, the relevant frequency response is $\widetilde H(\omega) := H(\omega) - 1$ (Section~\ref{sec:residual-filter}); the natural dataset-level statistic is therefore the \emph{residual passband fraction}, the proportion of average Welch-estimated periodogram power lying in frequencies where $\abs{\widetilde H(\omega)}^2$ exceeds its median over $[0, 1/2]$.

The empirical analysis below uses the \emph{full}-filter passband indicator $\mathbb{1}[\abs{H(\omega)}^2 > \mathrm{med}(\abs{H}^2)]$ as a proxy for the residual indicator. For our specific $d=4$, $s=16$ filter, $\abs{H}^2$ and $\abs{\widetilde H}^2$ have the same poles and qualitatively similar passband structure (both attenuate the harmonics $\omega_k = k/(2s)$ of Theorem~\ref{thm:freq-response}, though with different gains: $\abs{H}^2 \in [1/64, 289/64]$, $\abs{\widetilde H}^2 \in [1/64, 81/64]$). Both proxies should detect the same direction of association with dataset spectral mass, but the magnitudes will differ; we treat the existing analysis as a coarse qualitative test.

Figure~\ref{fig:spectral-correlate} shows the resulting scatter, where each point is one $(\text{dataset}, \text{frequency})$ pair. OLS regression of dataset-level $\Delta\text{MASE}\%$ on the full-filter passband fraction yields slope $\hat\beta_1 = +2.31$ percentage points per unit passband fraction, Pearson $\hat r = +0.10$, and a permutation-test $p$-value of $0.54$. The direction is consistent with the spectral hypothesis but the test does not reach $\alpha = 0.05$. We interpret this as \emph{directionally consistent with} the spectral mechanism---neither confirming nor falsifying it: the indicator-based proxy discards magnitude information, the dataset-level sample size ($n = 39$) is small, and many uncontrolled confounders (series length, drift, predictability) are absorbed into the residual.

\begin{figure}[h]
\centering
\includegraphics[width=0.85\linewidth]{figures/spectral_correlate.pdf}
\caption{Per-dataset spectral structure vs.\ HD10 improvement. Each point is one of $39$ unique GIFT-Eval $(\text{dataset}, \text{frequency})$ pairs; the $x$-axis is the full-filter passband fraction (a proxy for the residual passband fraction). The OLS slope of $+2.31$ percentage points improvement per unit passband fraction is positive (direction predicted by the spectral mechanism), but Pearson $\hat r = +0.10$ and permutation $p = 0.54$ do not reach $\alpha = 0.05$.}
\label{fig:spectral-correlate}
\end{figure}

\subsection{Mathematical Formulation of the Spectral Mechanism}
\label{sec:freq-overlap}

The passband-fraction proxy used in Section~\ref{sec:spectral-correlate} is a discretized version of a more natural continuous statistic that we now derive from Theorem~\ref{thm:freq-response} and Plancherel's theorem.

Let $S_x(\omega)$ denote the (unit-normalized) power spectral density of a wide-sense-stationary process $x$, and let $\abs{\widetilde H(\omega)}^2$ be the squared magnitude response of the \emph{residual} filter $p(B_s) - 1$ (Proposition~\ref{prop:residual-filter}); recall $\widetilde H(\omega) = H(\omega) - 1$. Define the continuous \emph{residual overlap}
\begin{equation}
    \widetilde O(x) \;:=\; \int_0^{1/2} S_x(\omega) \, \abs{\widetilde H(\omega)}^2 \, d\omega \;=\; \E_{\omega \sim S_x}\big[\,\abs{\widetilde H(\omega)}^2\,\big].
    \label{eq:spectral-overlap}
\end{equation}

\begin{proposition}[Plancherel Identity for Residual-Filtered Signal Power]
\label{prop:plancherel}
Let $x$ be wide-sense stationary with $\E[x[t]] = 0$ and PSD $S_x$, and let $\tilde h = (p(B_s) - 1) x$ be the residual-filtered signal. Then
\[
    \E\big[\tilde h[t]^2\big] \;=\; \int_{-1/2}^{1/2} S_x(\omega) \, \abs{\widetilde H(\omega)}^2 \, d\omega \;=\; 2 \, \widetilde O(x).
\]
That is, $\widetilde O(x)$ is exactly the post-residual-filter signal power (up to the symmetry factor $2$ for real signals).
\end{proposition}

\begin{proof}
Standard. By the convolution theorem, the autocorrelation of $\tilde h$ satisfies $R_{\tilde h}[\tau] = (R_x * (\tilde h * \tilde h^{(-)}))[\tau]$ where $\tilde h^{(-)}[k] = \tilde h[-k]$, and Fourier-transforming both sides gives $S_{\tilde h}(\omega) = S_x(\omega) \abs{\widetilde H(\omega)}^2$. Integrating $S_{\tilde h}$ over one full period yields the variance of $\tilde h$.\qedhere
\end{proof}

The passband-fraction proxy of Section~\ref{sec:spectral-correlate} replaces the magnitude $\abs{\widetilde H(\omega)}^2$ by an indicator at its median; the existing analysis additionally substitutes $\abs{H(\omega)}^2$ for $\abs{\widetilde H(\omega)}^2$. Both substitutions discard magnitude information but preserve the qualitative passband structure (since $H$ and $\widetilde H = H - 1$ share the same critical frequencies; their gains differ but their high-/low-gain band locations agree). The continuous overlap $\widetilde O(x)$ is the post-residual-filter signal power.

\begin{proposition}[Direction of the Effect]
\label{prop:overlap-direction}
Under the spectral hypothesis (Section~\ref{sec:precondition}), the per-dataset relative MASE improvement $\Delta(x)$ is monotone non-decreasing in $\widetilde O(x)$. Equivalently, OLS regression of $\Delta(x)$ on $\widetilde O(x)$ should produce a positive population slope $\beta_1 \geq 0$.
\end{proposition}

\paragraph{Empirical test (proxy variant).}
Using the existing full-filter passband-fraction data on $n = 39$ unique $(\text{dataset}, \text{frequency})$ pairs as a proxy for $\widetilde O(x)$, OLS regression yields slope $\hat\beta_1 = +2.31$ percentage points per unit passband fraction, Pearson $\hat r = +0.10$, and a permutation-test $p$-value of $0.54$. The direction is consistent with the hypothesis but does not reach $\alpha = 0.05$; bootstrap $95\%$ CIs are wide ($\hat r \in [-0.21, +0.35]$). We interpret this as \emph{directionally consistent with} the spectral mechanism but not strongly confirming it: both the residual-vs-full proxy substitution and the indicator-vs-continuous tradeoff discard magnitude information; the dataset-level sample size is small; and many uncontrolled confounders (series length, drift, predictability) are absorbed into the residual. A more powerful test would re-compute $\widetilde O(x)$ from~\eqref{eq:spectral-overlap} using the residual-filter magnitude with a continuous (rather than indicator) test statistic; we leave that refinement to follow-up work.

\paragraph{What we can conclude.}
The cross-domain pattern (Section~\ref{sec:discussion}) provides a strict refinement: \emph{within} the high-spectral-overlap subgroup (transport, energy, IT/cloud, demographics, M4-competition, ETT industrial), the per-domain improvement is uniformly $\geq +1.5\%$ with all but two domain $p$-values $< 10^{-4}$ in the workshop submission's domain-stratified sign tests; \emph{within} the low-spectral-overlap subgroup (weather, healthcare), improvements are within $\pm 0.4\%$ and not significantly different from zero. This stratified analysis, while less mathematically clean than a continuous regression, captures the spectral mechanism's prediction qualitatively at high effective sample size.

\subsection{FEV-Bench Cross-Benchmark Validation}
\label{sec:fevbench}

To verify that our findings generalize beyond GIFT-Eval, we evaluate on the independent \citet{das2024decoder} FEV-Bench (100 tasks). Following the same protocol as Section~\ref{sec:main-results}, we report normalized geomean MASE and FEV-Bench's scaled quantile loss (SQL):

\begin{table}[h]
\centering
\small
\caption{FEV-Bench results (5-seed mean for \HD{}, single-seed for \Baseline{}).}
\label{tab:fevbench}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{Geomean MASE} & \textbf{SQL} & \textbf{Wins/100} \\
\midrule
\HD{} (Ours) & $\mathbf{1.2532 \pm 0.0064}$ & $\mathbf{1.0236 \pm 0.0053}$ & $\mathbf{72}$ \\
\Baseline{} & $1.2908$ & $1.0562$ & --- \\
\midrule
$\Delta\%$ vs $\Baseline{}$ & $-2.91\%$ & $-3.09\%$ & --- \\
\bottomrule
\end{tabular}
\end{table}

The FEV-Bench improvements ($-2.91\%$ MASE, $-3.09\%$ SQL, $72/100$ wins) are within the BC$_a$ confidence interval of the GIFT-Eval improvement, and the per-degree sweep on FEV-Bench reproduces the GIFT-Eval pattern: every $d \geq 2$ beats baseline, the curve is shallow, and $d \in \{4, 6, 8\}$ are tied near the optimum. Two limitations of this comparison are worth noting. First, the FEV-Bench baseline is a single-seed run, while \HD{} is a 5-seed mean; we cannot quantify cross-seed baseline variability on FEV-Bench, so the direct seed-matched paired test we use for GIFT-Eval is not available here. Second, the comparison shares the training data (LOTSA) and pretraining protocol with the GIFT-Eval comparison; FEV-Bench is independent at the \emph{evaluation} level (different tasks, different metric) but not at the training-data level. We therefore present FEV-Bench as \emph{supportive cross-benchmark validation}---directionally consistent with the GIFT-Eval finding---rather than as an independent inferential test.

\subsection{Context Length and Learning-Rate Robustness}

\paragraph{Context length.} Across context lengths $\{512, 1000, 2000, 4000\}$, the relative improvement is approximately constant at $\sim 5\%$ (Appendix~\ref{app:context}). Notably, \HD{} at context $2000$ outperforms \Baseline{} at context $4000$ ($0.833$ vs.\ $0.867$), suggesting that input preconditioning can substitute for doubling the context window.

\paragraph{Learning rate.} \HD{} is insensitive to learning rate ($0.1\%$ MASE change between $\eta = 10^{-3}$ and $2 \times 10^{-3}$), while \Baseline{} varies by $2.27\%$. At matched optimal learning rate, \HD{} still wins significantly ($280/485$, $p=3.8 \times 10^{-4}$; Appendix~\ref{app:matchedlr}).

\subsection{Extended Training ($100$K Steps)}
\label{sec:100k}

\begin{table}[h]
\centering
\small
\caption{100K-step training, 5 seeds. The benefit grows with training budget; capacity controls actively hurt.}
\label{tab:100k}
\begin{tabular}{lcc}
\toprule
\textbf{Method} & \textbf{GeoMean MASE} & \textbf{$\Delta$\% vs \Baseline{}} \\
\midrule
\HD{} (Ours) & $\mathbf{0.844}$ & $\mathbf{-3.88\%}$ \\
\Baseline{} & $0.878$ & --- \\
\Dup{} & $0.886$ & $+0.86\%$ \\
\Zero{} & $0.894$ & $+1.73\%$ \\
\bottomrule
\end{tabular}
\end{table}

Two observations: the benefit of polynomial preconditioning \emph{grows} with training budget ($-3.9\%$ at $100$K vs.\ $-2.9\%$ at $10$K), and the capacity controls that were near-neutral at $10$K now actively hurt. The \Baseline{} also shows higher across-seed variance at $100$K (range $0.863$--$0.909$), consistent with late-training instability that the preconditioning channel appears to regularize.

%==============================================================================
\section{Statistical Methodology}
\label{sec:statistics}
\label{sec:stats}
%==============================================================================

This section presents the statistical methodology underlying the inferential claims in Sections~\ref{sec:main-results} and~\ref{sec:100k}. The per-configuration MASE measurements are not independent across seeds (same configuration, same data), so we pair across seed $\times$ configuration before aggregating.

\subsection{Paired Sign Tests and Pooling Across Seeds}

For each pairing of method $m$ versus baseline $B$ and each (seed, config) pair $(s, c)$, define the win indicator $w_{sc}^{(m)} = \mathbb{1}[\text{MASE}_{m, s, c} < \text{MASE}_{B, s, c}]$. Under the null hypothesis of no improvement, $w_{sc}^{(m)} \sim \text{Bernoulli}(1/2)$. Were the $485 = 5 \times 97$ observations independent under the null, the pooled sum $W^{(m)} = \sum_{s, c} w_{sc}^{(m)}$ would follow $\text{Binomial}(485, 1/2)$, and the corresponding $p$-value would be exact.

\paragraph{Independence caveat.}
Independence across the $5 \times 97$ outcomes is not strictly justified: each of the $97$ configurations is reused across all $5$ seeds with the same training data and evaluation setup, so the $5$ wins for a fixed configuration are not independent draws (the configuration's intrinsic difficulty and noise enter every seed). Per-seed sign tests, conditional on a fixed seed, do not have this issue: within a seed, the $97$ configurations are paired and the wins can be treated as $97$ approximately-independent Bernoulli draws under the null. We therefore report both per-seed and pooled sign-test $p$-values; the per-seed tests are more conservative (smaller effective sample size, but proper independence assumption), and the pooled tests assume an exchangeable-by-configuration null that may overstate certainty if the configuration-level dependence is large. A cluster-robust sensitivity analysis would either bootstrap by configuration (resampling configurations with replacement) or use a configuration-clustered standard error; we leave a formal cluster bootstrap to follow-up work.

We report two-sided $p$-values throughout. For \HD{} vs.\ \Baseline{}, $W = 329$, yielding pooled $p = 1.5 \times 10^{-15}$ under the iid null and four-of-five-seeds significant at $\alpha = 0.05$ under per-seed tests with Holm correction (Appendix~\ref{app:seeds}). Both formulations agree on the qualitative conclusion that \HD{} systematically beats \Baseline{}, but the numerical $p$-value is more robust under the per-seed tests.

\subsection{Multiple-Testing Correction}

We correct for testing multiple methods (here $\HD{}$, \Zero{}, \Dup{} versus \Baseline{}, plus several ablation comparisons) using the Holm--Bonferroni step-down procedure~\citep{holm1979simple}, which controls familywise error rate (FWER) at level $\alpha$. Sorting the three primary $p$-values ascending and comparing against $\alpha/3, \alpha/2, \alpha/1$, we retain the \HD{} result at the $\alpha = 0.05$ level (adjusted $p < 10^{-14}$) and would retain \Zero{} at $\alpha = 0.10$ (adjusted $p = 0.13$; not significant at $0.05$).

For per-seed analyses ($5$ seeds for \HD{} vs.\ \Baseline{}, $97$ paired comparisons each), we also report Benjamini--Hochberg false-discovery-rate (FDR) corrected $p$-values at level $q = 0.05$~\citep{benjamini1995controlling}. Holm-corrected per-seed two-sided $p$-values are: $1.5 \times 10^{-3}$ (s0), $0.31$ (s1), $5.9 \times 10^{-5}$ (s2), $5.8 \times 10^{-9}$ (s7), $2.9 \times 10^{-2}$ (s42); four of five seeds remain individually significant at $\alpha = 0.05$, and the result is robust to the most conservative FWER correction over per-seed tests (Appendix~\ref{app:seeds}).

\subsection{Bootstrap Confidence Intervals}

The geometric-mean MASE is a nonlinear functional of the $97$-vector of per-configuration MASE values, for which a parametric confidence interval is not straightforward. We compute a $95\%$ BC$_a$ (bias-corrected, accelerated) nonparametric bootstrap interval~\citep{efron1987better} on the pooled 5-seed relative MASE improvement, $\exp(\bar{\log(\text{HD}/\text{BL})}) - 1$, by resampling the $485$ paired observations with replacement $5{,}000$ times. The observed relative improvement is $-2.90\%$, with $95\%$ BC$_a$ CI $[-3.71\%, -2.12\%]$ (more negative = larger MASE reduction). The interval excludes zero with substantial margin, providing a confidence-interval-based confirmation of the sign-test result.

\subsection{Effect Sizes}

Cohen's $d$ for per-configuration log-MASE, computed on the $n = 485$ paired log-ratios, is $d = +0.31$ (small-to-medium), and Cliff's $\delta = +0.36$ (medium). Both quantify the systematic nature of the improvement despite modest per-configuration magnitudes: the gain is small \emph{per configuration}, large \emph{across configurations}, consistent with a broadly-applicable architectural intervention.

\subsection{Power Analysis}

We compute power for the paired sign test directly, without relying on Cohen's-$d$ normal approximations. Under an alternative in which the per-configuration win probability is $\theta = 329/485 = 0.678$ (the observed pooled rate against \Baseline{}), the test rejects at two-sided $\alpha = 0.05$ when $W \geq W_{\alpha/2}$ for $W \sim \text{Binomial}(n, 1/2)$. Using a normal approximation to the binomial, the test achieves power $1 - \beta = 0.80$ at $n \approx \big((z_{\alpha/2} + z_{1-\beta}) / (2(\theta - 0.5))\big)^2 \approx ((1.96 + 0.84) / 0.356)^2 \approx 62$ paired observations, and $0.95$ at $n \approx 109$. Our pooled sample of $485$ comfortably exceeds both thresholds, and each per-seed sample of $97$ comparisons exceeds the $80\%$-power threshold individually. The detection of the effect is therefore not power-limited under either the pooled or per-seed framing.

\subsection{Capacity-Matched Controlled Comparisons}

The \Zero{} and \Dup{} conditions are \emph{capacity-matched controls}: they differ from \HD{} only in the content of the auxiliary preconditioning channel, with identical architecture, identical parameter count, and (by design) identical optimisation budget. We do not call this a controlled experiment in the strict causal-identification sense: the three conditions also induce different optimisation dynamics (different gradient flows through the input projection), so the observed differences in MASE could in principle reflect interactions between content and optimisation rather than the content alone. What the comparison \emph{does} establish, conservatively, is that the gain over \Baseline{} is not simply attributable to the additional $12$K parameters in the input projection: \Dup{} has those parameters and underperforms \Baseline{}, while \Zero{} has them and is close to neutral, so the parameter overhead alone does not explain \HD{}'s improvement. The estimated $\HD{} - \Dup{}$ MASE difference is $-3.3\%$ with $95\%$ BC$_a$ CI $[-3.9\%, -2.6\%]$.

%==============================================================================
\section{Related Work}
\label{sec:related}
%==============================================================================

\paragraph{Time series foundation models.}
Transformer-based universal forecasters have proliferated~\citep{woo2024unified, liu2024moirai2, das2024decoder, ansari2024chronos, goswami2024moment, auer2025tirex}. Recent work critically examines the zero-shot claims~\citep{zhang2025tsfmready}, arguing that simple PCA+linear baselines can be competitive; our method strengthens compact foundation models in exactly this small-model regime. Most architectural innovations in the space (rotary embeddings, attention variants, mixture-of-experts~\citep{liu2024moirai2}) target the transformer stack; input preconditioning is orthogonal, acting on the input representation before any attention layer.

\paragraph{Patching and overlap.}
Patch-based architectures~\citep{nie2023patchtst, dosovitskiy2021image} dominate TSFMs. PatchTST uses overlapping patches to partially mitigate the cross-patch bottleneck; our approach achieves a similar effect at lower cost (no FLOP increase, no parameter increase in the encoder) and is complementary (one could combine both).

\paragraph{Spectral methods in sequence modeling.}
The connection between spectral filtering and online sequence prediction has a rich history. \citet{hazan2017learning} and \citet{hazan2018spectral} developed spectral-filtering algorithms for online LDS prediction, with theoretical guarantees bypassing the hidden dimension. \citet{marsden2024universal} unify and generalize this line via polynomial preconditioning, which directly motivates our filter design.

\paragraph{Classical FIR filter theory.}
FIR filter design with Chebyshev polynomials is a classical topic~\citep{parks1972chebyshev, oppenheim1999discrete}; our contribution is to bring this to bear on the input representation of a modern transformer forecaster, rather than as a post-hoc smoother.

%==============================================================================
\section{Discussion and Limitations}
\label{sec:discussion}
%==============================================================================

\paragraph{When does preconditioning help?}
The spectral story predicts---and the spectral-correlate analysis (Section~\ref{sec:spectral-correlate}) is directionally consistent with---that datasets whose power spectrum aligns with the filter's attenuation minima benefit least, and those whose mass aligns with passbands benefit most. The cross-domain pattern is consistent with this prediction in a coarse sense: transport and energy domains ($+4$ to $+7\%$ gains) have strong autocorrelation at the $32$- and $64$-sample lookback scales of the filter at $15$-minute resolution (corresponding to $8/16$-hour intra-day structure); weather and healthcare, dominated by much-longer or stochastic correlations, show no significant improvement. We do not claim that this matches the precise filter-frequency calculation in Theorem~\ref{thm:freq-response}; the $32$- and $64$-step filter lags and the $\omega = 0, 1/32, 1/16, \ldots$ attenuation frequencies are distinct mathematical objects, and the cross-domain evidence cannot distinguish their respective contributions.

\paragraph{Model scale.}
We find that the benefit of input preconditioning is most pronounced at the small ($11.4$M) Moirai-2 scale; at the base ($46$M) scale, the improvement is seed-dependent and not consistently significant. This is consistent with a capacity-efficiency interpretation: when a model has enough parameters to discover the relevant cross-patch correlations via attention, the inductive bias of a fixed Chebyshev preconditioner adds less value.

\paragraph{Learning rate.}
At matched optimal learning rate, the effect attenuates to $+0.72\%$ ($p = 3.8 \times 10^{-4}$). This suggests that part of \HD{}'s observed benefit at the default learning rate stems from its greater insensitivity to optimization hyperparameters. Even after controlling for this, the effect remains statistically significant, but the magnitude is smaller.

\paragraph{Limitations.}
We focused on univariate, single-variate forecasting; extensions to multivariate and probabilistic forecasting are left as future work. The Chebyshev choice was validated by ablation against a handful of alternatives (Legendre, EMA, finite differences); exhaustive search over the monic-degree-$d$ family is computationally infeasible but could be approximated by closed-form optimal linear filters (Wiener filters) in a synthetic setting.

%==============================================================================
\section{Conclusion}
%==============================================================================

We studied polynomial input preconditioning for patch-based universal time series forecasters, a minimal architectural intervention that injects cross-patch temporal information via a fixed Chebyshev FIR filter at $0.11\%$ parameter overhead. Bridging the classical Chebyshev minimax theorem, the recent framework of universal sequence preconditioning~\citep{marsden2024universal}, and modern patch-based transformer forecasters, we established a closed-form frequency response for our specific filter, proved a BIBO-stability lemma, and validated the theoretical predictions empirically on synthetic LDS trajectories and on the GIFT-Eval benchmark ($2.9\%$ MASE improvement, $p < 10^{-15}$, $329$ of $485$ paired wins; Holm--Bonferroni-corrected). Controlled capacity experiments confirmed that the improvement is driven by the polynomial content of the preconditioning channel rather than by extra parameters. Our spectral-correlate analysis is directionally consistent with the prediction that cross-dataset variation in improvement aligns with dataset-level frequency structure, but does not confirm the mechanism at standard significance levels at the granularity of $39$ unique source datasets.

\newpage
\bibliographystyle{plainnat}
\bibliography{references}

\newpage
\appendix

\section{Per-Seed Sign Tests with Multiple-Testing Correction}
\label{app:seeds}

Table~\ref{tab:perseed-corrected} reports the per-seed paired sign-test outcomes for \HD{} vs.\ \Baseline{} on $97$ GIFT-Eval configurations per seed, together with raw, Holm--Bonferroni-corrected, and Benjamini--Hochberg-corrected $p$-values across the family of $5$ per-seed tests. Holm--Bonferroni controls the family-wise error rate at the nominal $\alpha$; BH controls the false-discovery rate at $q$. Four of five seeds reject the null at the corrected $\alpha = 0.05$ level under both procedures.

\begin{table}[h]
\centering
\caption{Per-seed sign tests for \HD{} vs.\ \Baseline{} ($n_c = 97$ configurations per seed, two-sided binomial). Corrections applied across the family of five per-seed tests.}
\label{tab:perseed-corrected}
\small
\begin{tabular}{rcccc}
\toprule
\textbf{Seed} & \textbf{Wins/97} & \textbf{$p_{\text{raw}}$} & \textbf{$p_{\text{Holm}}$} & \textbf{$p_{\text{BH}}$} \\
\midrule
0 & 66 & $4.9 \times 10^{-4}$ & $1.5 \times 10^{-3}$ & $8.2 \times 10^{-4}$ \\
1 & 54 & $0.31$               & $0.31$               & $0.31$ \\
2 & 70 & $1.5 \times 10^{-5}$ & $5.9 \times 10^{-5}$ & $3.7 \times 10^{-5}$ \\
7 & 78 & $1.2 \times 10^{-9}$ & $5.8 \times 10^{-9}$ & $5.8 \times 10^{-9}$ \\
42 & 61 & $1.4 \times 10^{-2}$ & $2.9 \times 10^{-2}$ & $1.8 \times 10^{-2}$ \\
\midrule
\textbf{Pooled} & \textbf{329/485} & $\mathbf{2.9 \times 10^{-15}}$ & --- & --- \\
\bottomrule
\end{tabular}
\end{table}

The pooled test treats the $485$ observations (5 seeds $\times$ 97 configs) as exchangeable Bernoulli draws under the null. The per-seed tests are individually conservative (each conditions on the seed). Both formulations agree on the qualitative conclusion that \HD{} systematically improves over \Baseline{}.

\section{Detailed Methodology}
\label{app:methodology}

\subsection{Training Protocol}

All experiments use identical training infrastructure on NVIDIA H100/A100 GPUs.

\begin{center}
\small
\begin{tabular}{ll}
\toprule
\textbf{Parameter} & \textbf{Value} \\
\midrule
Architecture & Moirai-2 Small (6 layers, $d=384$, $d_{\text{ff}}=1024$, $P=16$) \\
Total parameters & 11.4M (Baseline), 11.4M $+$ 12K (\HD{}) \\
Training data & LOTSA v1, Moirai-2 config (weighted, variate-proportional) \\
Steps (10K runs) & 100 epochs $\times$ 100 batches $=$ 10{,}000 steps \\
Steps (100K runs) & 1{,}000 epochs $\times$ 100 batches $=$ 100{,}000 steps \\
Batch size & 256 \\
Optimizer & AdamW ($\beta_1=0.9$, $\beta_2=0.98$, $\epsilon=10^{-6}$, wd $=0.1$) \\
LR schedule & Cosine annealing, peak $\eta = 10^{-3}$, min $\eta = 0$ \\
Warmup & 1{,}000 steps (linear) \\
Precision & bf16 mixed precision \\
Anomaly filtering & $z$-score threshold $=8.0$, variance ratio $=0.0$ (disabled) \\
Random seeds & 0, 1, 2, 7, 42 (5 seeds for main results) \\
\bottomrule
\end{tabular}
\end{center}

\subsection{Evaluation Protocol}

All evaluations use the GIFT-Eval benchmark \citep{vijay2024gifteval} with 97 dataset$\times$horizon configurations:
\begin{itemize}[leftmargin=1.5em]
    \item Context length: 4{,}000 tokens (maximum supported)
    \item Patch size: 32 for most frequencies, 8 for quarterly (following Moirai conventions)
    \item Batch size: 64
    \item Metric: Normalized MASE (each configuration's MASE divided by the seasonal naive baseline's MASE)
    \item Aggregation: geometric mean across 97 configurations
    \item Reported quantities: cross-seed mean and standard deviation, per-seed MASE, and per-configuration win counts (out of 97 per seed; $5 \times 97 = 485$ when pooled across seeds)
\end{itemize}

\subsection{Preconditioning Channel Implementation}

The Chebyshev polynomial $T_d(z)$ is expanded in the monomial basis to obtain FIR filter coefficients $c_0, \ldots, c_d$. The preconditioned signal for time step $t$ is
\[
    \tilde{h}[t] \;=\; \left(\sum_{k=0}^{d} c_k \, x[t - ks]\right) - x[t],
\]
computed over the full (normalized, zero-mean unit-variance) time series before patching. Unobserved time steps are masked to zero. During training with channel dropout $p$, each patch's preconditioning channel independently has probability $p$ of being replaced with zeros. At inference, the full preconditioning channel is always provided.

The preconditioning channel is concatenated with the target and mask channels before the input projection. The input projection is the only architectural change relative to Baseline; all downstream layers (transformer encoder, output projection) are identical.

%==============================================================================
\section{Capacity Controls}
\label{app:capacity}
%==============================================================================

The Zero and Duplicate conditions serve as capacity controls: they have the \emph{same architecture and parameter count} as \HD{}, differing only in the content of the extra channel.

\begin{table}[h]
\centering
\caption{Capacity control experiments at 10K steps (5 seeds, $\eta = 10^{-3}$). Same architecture width, different channel content.}
\label{tab:capacity-10k}
\small
\begin{tabular}{lcccc}
\toprule
\textbf{Method} & \textbf{Content} & \textbf{Mean MASE} & \textbf{Std} & \textbf{Wins/485} \\
\midrule
\textbf{\HD} & Chebyshev $\tilde T_4(B^{16})$ & \textbf{0.8368} & 0.0083 & \textbf{329} \\
Zero & All zeros & 0.8578 & 0.0102 & 262 \\
Baseline & No extra channel & 0.8617 & 0.0062 & --- \\
Duplicate & Copy of input & 0.8650 & 0.0081 & 217 \\
\bottomrule
\end{tabular}
\end{table}

\begin{itemize}[leftmargin=1.5em]
    \item \textbf{Duplicate channel hurts}: win rate $44.7\%$, \emph{worse} than the $50\%$ expected by chance.
    \item \textbf{Zero channel is neutral}: win rate $54.0\%$, tiny effect ($-0.45\%$).
    \item \HD{} vs.\ Duplicate: $336/485$ wins (\HD{} beats Duplicate on every seed).
    \item \HD{} vs.\ Zero: $323/485$ wins (\HD{} beats Zero on every seed).
\end{itemize}

At 100K steps, both controls actively hurt: Duplicate degrades by $0.9\%$ and Zero by $1.7\%$ relative to Baseline. Full table in Appendix~\ref{app:100k}.

%==============================================================================
\section{Filter Ablations}
\label{app:ablations}
%==============================================================================

All ablations use seed 0 with $10\%$ channel dropout unless otherwise noted; the polynomial-degree sweep is the exception and is reported over $5$ seeds each.

\subsection{Polynomial Degree (Chebyshev, $s=16$, $10\%$ dropout)}

\begin{table}[h]
\centering
\small
\begin{tabular}{lccc}
\toprule
\textbf{Degree} & \textbf{Residual lags} & \textbf{MASE (mean $\pm$ std, $n=5$)} & \textbf{$\Delta\%$ vs.\ BL} \\
\midrule
2 & 1 (at $2P$) & $0.8502 \pm 0.0125$ & $-1.33\%$ \\
3 & 1 (at $2P$) & $0.8422 \pm 0.0053$ & $-2.27\%$ \\
\textbf{4} & 2 (at $2P$, $4P$) & $\mathbf{0.8368 \pm 0.0092}$ & $\mathbf{-2.89\%}$ \\
5 & 2 (at $2P$, $4P$) & $0.8423 \pm 0.0078$ & $-2.25\%$ \\
6 & 3 (at $2P$, $4P$, $6P$) & $0.8400 \pm 0.0105$ & $-2.52\%$ \\
7 & 3 (at $2P$, $4P$, $6P$) & $0.8470 \pm 0.0110$ & $-1.71\%$ \\
8 & 4 (at $2P$, $4P$, $6P$, $8P$) & $0.8414 \pm 0.0050$ & $-2.36\%$ \\
\midrule
Baseline & --- & $0.8617 \pm 0.0062$ & --- \\
\bottomrule
\end{tabular}
\end{table}

Every $d \geq 2$ beats Baseline. The mean curve is shallow: $d=4$ achieves the lowest geomean MASE but $d \in \{3, 5, 6, 8\}$ are within roughly one seed-to-seed standard deviation of it. The extreme $d=2$ has only one non-trivial residual lag (at $2P$). Note that under the reciprocal-monic convention, the residual filter for any odd $d$ contains only even-lag terms, so the lag structure of $d \in \{3, 5, 7\}$ matches that of $d-1 \in \{2, 4, 6\}$ respectively, with different coefficients; this can be verified by direct expansion of $\tilde T_d^*(z) - 1$.

\subsection{Filter Stride (Chebyshev $d=4$, $10\%$ dropout)}

\begin{table}[h]
\centering
\small
\begin{tabular}{ccccc}
\toprule
\textbf{Stride} & \textbf{Lookback} & \textbf{MASE} & \textbf{$\Delta\%$ vs.\ BL} & \textbf{Wins/97} \\
\midrule
\textbf{16 ($=P$)} & 32, 64 steps & \textbf{0.8234} & $\mathbf{-4.99\%}$ & \textbf{66} \\
4 & 8, 16 steps & 0.8406 & $-3.00\%$ & 57 \\
8 & 16, 32 steps & 0.8518 & $-1.71\%$ & 62 \\
32 & 64, 128 steps & 0.8484 & $-2.10\%$ & 63 \\
8+16 (dual) & 16, 32, 64 steps & 0.8389 & $-3.20\%$ & 62 \\
\bottomrule
\end{tabular}
\end{table}

Stride $s = 16 = P$ is optimal: filter taps align exactly with patch boundaries, providing maximally informative cross-patch lookback.

\subsection{Filter Basis ($d=4$, $s=16$, $10\%$ dropout)}

\begin{table}[h]
\centering
\small
\begin{tabular}{lcccc}
\toprule
\textbf{Basis} & \textbf{MASE} & \textbf{$\Delta\%$} & \textbf{Wins vs.\ BL} & \textbf{Wins vs.\ Cheby} \\
\midrule
\textbf{Chebyshev} $\tilde T_4^*$ & \textbf{0.8234} & $\mathbf{-4.99\%}$ & 66/97 & --- \\
EMA & 0.8329 & $-3.89\%$ & 65/97 & 41/97 \\
Legendre $\tilde P_4^*$ & 0.8400 & $-3.07\%$ & 62/97 & 30/97 \\
Legendre $\tilde P_6^*$ & 0.8397 & $-3.10\%$ & 64/97 & 50/97 \\
Finite differences & 0.8569 & $-1.12\%$ & 54/97 & 29/97 \\
Duplicate & 0.8583 & $-0.96\%$ & 42/97 & 27/97 \\
\bottomrule
\end{tabular}
\label{tab:basis-ablation}
\end{table}

The basis ordering Chebyshev $>$ EMA $>$ Legendre $>$ finite differences confirms the theoretical prediction of Section~\ref{sec:basis-comparison}: the family that minimizes the $L^\infty$ norm on $[-1,1]$ outperforms families optimised for other norms.

\subsection{Channel Dropout Rate (Chebyshev $d=4$, $s=16$)}

\begin{table}[h]
\centering
\small
\begin{tabular}{cccc}
\toprule
\textbf{Dropout} & \textbf{MASE} & \textbf{$\Delta\%$ vs.\ BL} & \textbf{Wins/97} \\
\midrule
$0\%$ (\Dnodrop)$^\dagger$ & 0.8378 & $-3.50\%$ & --- \\
$2\%$ & 0.8388 & $-3.20\%$ & 63 \\
$3\%$ & 0.8497 & $-1.95\%$ & 55 \\
\textbf{$10\%$ (\HD{})} & \textbf{0.8234} & $\mathbf{-4.99\%}$ & \textbf{66} \\
$20\%$ & 0.8422 & $-2.81\%$ & 67 \\
$30\%$ & 0.8559 & $-1.23\%$ & 61 \\
\bottomrule
\end{tabular}\\[2pt]
{\footnotesize $^\dagger$\Dnodrop{} MASE is the 5-seed mean. \Dnodrop{} and \HD{} are essentially tied; dropout helps marginally.}
\end{table}

\subsection{Learned FIR Coefficients (seed 0)}

\begin{table}[h]
\centering
\small
\begin{tabular}{lccc}
\toprule
\textbf{Initialization} & \textbf{Learned coefficients} & \textbf{MASE} & \textbf{$\Delta\%$ vs.\ BL} \\
\midrule
Chebyshev init & $[-0.026, -1.032, -0.123, 0.007]$ & 0.8332 & $-3.85\%$ \\
Zero init & $[0.308, 0.177, 0.057, 0.045]$ & 0.8358 & $-3.57\%$ \\
\textbf{Fixed Chebyshev} & $[0, -1, 0, 0.125]$ (not learned) & \textbf{0.8234} & $\mathbf{-4.99\%}$ \\
\bottomrule
\end{tabular}
\label{tab:learned-fir}
\end{table}

The two learned filters converge to distinct basins (high-pass vs.\ smoothing). Both beat Baseline but are slightly worse than fixed Chebyshev (51/97 head-to-head wins for fixed). Fixing the polynomial provides an inductive bias the model would otherwise have to discover.

\subsection{Latent Preconditioning (seed 0)}

Applying the polynomial filter \emph{after} the input projection (in latent space) rather than before patching:

\begin{table}[h]
\centering
\small
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{Filter location} & \textbf{MASE} & \textbf{vs.\ BL} \\
\midrule
\HD{} (input-level) & Before patching & 0.8234 & $-4.99\%$ \\
Latent $s=1$ & After input projection & 1.1538 & $+33.1\%$ \\
Latent $s=4$ & After input projection & 1.2243 & $+41.3\%$ \\
\bottomrule
\end{tabular}
\end{table}

Latent preconditioning is catastrophic. The filter must operate on the raw time series, not on learned embeddings: the spectral structure is destroyed by the input projection's nonlinear mixing.

%==============================================================================
\section{Domain and Frequency Analysis}
\label{app:domain}
%==============================================================================

\begin{table}[h]
\centering
\caption{Domain analysis (\HD{} vs.\ BL, 5 seeds pooled, 485 observations).}
\small
\begin{tabular}{lccc}
\toprule
\textbf{Domain} & \textbf{$n$} & \textbf{$\Delta\%$} & \textbf{Wins} \\
\midrule
Demographics & 15 & $+9.83\%$ & 13/15 \\
Transport & 55 & $+6.94\%$ & 47/55 \\
Energy & 80 & $+3.97\%$ & 56/80 \\
M4 Competition & 30 & $+3.83\%$ & 23/30 \\
IT/Cloud & 120 & $+2.23\%$ & 81/120 \\
ETT (Industrial) & 80 & $+1.50\%$ & 54/80 \\
Retail/Sales & 20 & $+0.85\%$ & 14/20 \\
Weather & 75 & $+0.34\%$ & 36/75 \\
Healthcare & 10 & $+0.24\%$ & 5/10 \\
\bottomrule
\end{tabular}
\end{table}

The benefit is consistent with the mechanism, although we caution that the relevant filter-level mechanism has two distinct manifestations: (a) the residual filter's \emph{lookback lags}, which are $2s = 32$ and $4s = 64$ time steps (this controls which past samples enter the residual channel directly); and (b) the residual filter's \emph{attenuation/passband frequencies}, which are at $\omega = 0, 1/32, 1/16, \ldots$ cycles per sample (this controls which frequencies the residual channel preserves vs.\ attenuates). The two are mathematically distinct objects and need not coincide with dataset-level periodicities. For $15$-minute data, the $32/64$-step lookbacks correspond to $8/16$ hours of context---useful for transport and energy demand, which exhibit autocorrelation across that range---while the filter's attenuation periods of $\infty, 8\,\text{h}, 4\,\text{h}, \ldots$ are a distinct set. For daily weather data, both manifestations point to $32$/$64$-day scales which are too coarse to capture weekly/seasonal patterns. The cross-domain pattern is therefore consistent with both lookback and frequency interpretations, but the data does not let us distinguish their contributions.

\begin{table}[h]
\centering
\caption{Frequency analysis (\HD{} vs.\ BL, 5 seeds pooled).}
\small
\begin{tabular}{lccc}
\toprule
\textbf{Frequency} & \textbf{$n$} & \textbf{$\Delta\%$} & \textbf{Wins} \\
\midrule
Sub-hourly & 180 & $+3.40\%$ & 121/180 \\
Hourly & 155 & $+3.80\%$ & 113/155 \\
Daily & 75 & $+0.76\%$ & 46/75 \\
Weekly & 40 & $+0.73\%$ & 29/40 \\
Monthly & 25 & $+3.41\%$ & 13/25 \\
Quarterly & 5 & $+0.24\%$ & 3/5 \\
Yearly & 5 & $+5.92\%$ & 4/5 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Prediction horizon.} The benefit increases with prediction length: $+1.4\%$ for short horizons to $+5.1\%$ for long horizons. Longer horizons require more temporal structure, which the preconditioning channel provides.

%==============================================================================
\section{Horizon Analysis: \Dnodrop{} vs.\ \HD{}}
\label{app:horizon}
%==============================================================================

The preconditioning filter for \Dnodrop{}/\HD{} ($d=4$, $s=16$) reduces to $\tilde h[t] = -x[t-32] + (1/8)\, x[t-64]$. We test whether \HD{}'s channel-dropout regularisation buys robustness on long horizons that \Dnodrop{} lacks.

\begin{table}[h]
\centering
\caption{\Dnodrop{} vs.\ \HD{} vs.\ Baseline by GIFT-Eval term.}
\small
\begin{tabular}{lccccccc}
\toprule
\textbf{Bin} & \textbf{$n$} & \textbf{BL} & \textbf{\Dnodrop} & \textbf{\HD} & \textbf{\Dnodrop{} $\Delta\%$} & \textbf{\HD{} $\Delta\%$} & \textbf{\Dnodrop{} better} \\
\midrule
short  & 55 & 0.8403 & \textbf{0.8252} & 0.8282 & $-1.79\%$ & $-1.44\%$ & $24/55$ \\
medium & 21 & 0.8592 & 0.8301 & \textbf{0.8231} & $-3.39\%$ & $-4.20\%$ & $9/21$ \\
long   & 21 & 0.9237 & 0.8802 & \textbf{0.8744} & $-4.71\%$ & $-5.34\%$ & $7/21$ \\
\midrule
ALL    & 97 & 0.8618 & 0.8379 & \textbf{0.8368} & $-2.77\%$ & $-2.90\%$ & $40/97$ \\
\bottomrule
\end{tabular}
\end{table}

On short horizons \Dnodrop{} and \HD{} are within $0.4\%$ of each other; on medium and long horizons (PL $\geq 480$) \HD{} beats \Dnodrop{} by $0.7$--$0.85\%$ relative MASE---the direction the dropout-as-mismatch-regulariser story predicts but small relative to the $\sim 4\%$ HD-vs-Baseline gap.

%==============================================================================
\section{Context-Length Analysis}
\label{app:context}
%==============================================================================

\begin{table}[h]
\centering
\caption{Context-length analysis (seed 0). The benefit is approximately constant ($\sim 5\%$) across context lengths.}
\small
\begin{tabular}{ccccc}
\toprule
\textbf{Context} & \textbf{BL} & \textbf{\HD} & \textbf{$\Delta\%$} & \textbf{Wins/97} \\
\midrule
512    & 0.9432 & 0.8986 & $-4.73\%$ & 62 \\
1{,}000  & 0.9166 & 0.8706 & $-5.02\%$ & 65 \\
2{,}000  & 0.8813 & 0.8331 & $-5.47\%$ & 71 \\
4{,}000  & 0.8666 & 0.8234 & $-4.99\%$ & 66 \\
\bottomrule
\end{tabular}
\end{table}

\HD{} at context length 2{,}000 (MASE $0.8331$) outperforms Baseline at context length 4{,}000 (MASE $0.8666$), suggesting that input preconditioning can substitute for doubling the context window.

%==============================================================================
\section{Matched Learning-Rate Analysis}
\label{app:matchedlr}
%==============================================================================

\begin{table}[h]
\centering
\caption{Matched-learning-rate comparison ($\eta = 2 \times 10^{-3}$, 5 seeds, 485 comparisons).}
\small
\begin{tabular}{ccccc}
\toprule
\textbf{Seed} & \textbf{BL@2e-3} & \textbf{\HD{}@2e-3} & \textbf{$\Delta\%$} & \textbf{Wins/97} \\
\midrule
0 & 0.8424 & 0.8339 & $+1.01\%$ & 57 \\
1 & 0.8442 & 0.8338 & $+1.23\%$ & 55 \\
2 & 0.8378 & 0.8468 & $-1.07\%$ & 46 \\
7 & 0.8339 & 0.8350 & $-0.13\%$ & 54 \\
42 & 0.8526 & 0.8307 & $+2.55\%$ & 68 \\
\midrule
\textbf{Mean} & \textbf{0.8422} & \textbf{0.8361} & \textbf{$+0.72\%$} & \textbf{280/485} \\
\textbf{Std} & 0.0071 & 0.0062 & $1.38\%$ & --- \\
\bottomrule
\end{tabular}
\end{table}

Baseline is sensitive to learning rate: $\eta = 2 \times 10^{-3}$ improves it by $2.27\%$. \HD{} is insensitive ($0.09\%$ difference between $\eta = 10^{-3}$ and $2 \times 10^{-3}$). At matched optimal LR, \HD{} still wins ($280/485$, $p = 3.8 \times 10^{-4}$).

%==============================================================================
\section{Multi-Scale Variants}
\label{app:multiscale}
%==============================================================================

We tested two multi-scale variants providing two preconditioning channels (degree 4 + degree 6):

\begin{table}[h]
\centering
\small
\begin{tabular}{lcccc}
\toprule
\textbf{Method} & \textbf{Mean} & \textbf{Std} & \textbf{vs.\ BL} & \textbf{Wins/485} \\
\midrule
\HD{} ($d=4$, $10\%$ drop) & 0.8368 & 0.0083 & $-2.89\%$ & 329 \\
MS46 ($d=4{+}6$, $0\%$ drop) & 0.8434 & 0.0123 & $-2.13\%$ & 330 \\
MSHD10 ($d=4{+}6$, $10\%$ drop) & 0.8437 & 0.0072 & $-2.09\%$ & 315 \\
\bottomrule
\end{tabular}
\end{table}

All three methods are essentially tied head-to-head. MSHD10 has the lowest cross-seed standard deviation. We chose \HD{} as the headline configuration because it has the simplest architecture (single channel) while matching the multi-scale variants' performance.

%==============================================================================
\section{100K Extended Training}
\label{app:100k}
%==============================================================================

\begin{table}[h]
\centering
\caption{Full 100K training budget with capacity controls (5 seeds). \HD{} vs.\ BL: $-3.9\%$; \HD{} vs.\ Duplicate: $-4.7\%$; \HD{} vs.\ Zero: $-5.5\%$.}
\small
\begin{tabular}{lcccccc}
\toprule
& \textbf{s0} & \textbf{s1} & \textbf{s2} & \textbf{s7} & \textbf{s42} & \textbf{Mean} \\
\midrule
Baseline & 0.9092 & 0.8920 & 0.8633 & 0.8641 & 0.8636 & 0.8784 \\
\textbf{\HD} & \textbf{0.8349} & \textbf{0.8402} & 0.8586 & \textbf{0.8450} & \textbf{0.8430} & \textbf{0.8443} \\
MSHD10 & 0.8266 & --- & 0.8432 & 0.8437 & --- & 0.8378$^\dagger$ \\
Duplicate & 0.8887 & 0.8740 & 0.8974 & 0.8794 & 0.8903 & 0.8860 \\
Zero & 0.8917 & 0.8585 & 0.8750 & 0.8825 & 0.9610 & 0.8937 \\
\midrule
\HD{} $\Delta\%$ vs.\ BL & $-8.2\%$ & $-5.8\%$ & $-0.5\%$ & $-2.2\%$ & $-2.4\%$ & $-3.9\%$ \\
\bottomrule
\end{tabular}\\[2pt]
{\footnotesize $^\dagger$MSHD10 mean over 3 available seeds (0, 2, 7).}
\end{table}

Two findings stand out. First, the benefit of polynomial preconditioning \emph{grows} with training budget: \HD{} improves by $3.9\%$ at 100K (vs.\ $2.9\%$ at 10K). Second, the capacity controls that were near-neutral at 10K now actively hurt: Duplicate degrades by $0.9\%$ and Zero by $1.7\%$. Baseline shows higher variance across seeds at 100K (seed 0 reaches $0.9092$ due to late-training instability), while \HD{} remains stable (range $0.83$--$0.86$).

%==============================================================================
\section{Inference-Time Channel-Content Ablation}
\label{app:inference-ablation}
%==============================================================================

A model trained with Chebyshev preconditioning (\HD{}) is evaluated with different channel content at inference time:

\begin{table}[h]
\centering
\caption{Inference-time content ablation (seed 0, \HD{} model). The ordering confirms the model has learned to exploit specific polynomial structure, not generic extra capacity.}
\small
\begin{tabular}{lcc}
\toprule
\textbf{Inference channel content} & \textbf{MASE} & \textbf{vs.\ Normal} \\
\midrule
Normal (Chebyshev) & 0.8234 & --- \\
No preconditioning channel (Baseline arch) & 0.8666 & $+5.2\%$ \\
Zeros & 0.9003 & $+9.3\%$ \\
Random Gaussian noise & 0.9427 & $+14.5\%$ \\
Duplicate (copy of input) & 1.0699 & $+29.9\%$ \\
\bottomrule
\end{tabular}
\end{table}

The ordering Normal $\gg$ Baseline $>$ Zero $>$ Random $>$ Duplicate confirms that the model exploits the specific polynomial structure. Random and duplicate channels are worse than no preconditioning at all, demonstrating that the model does not simply use the extra channel for generic capacity.

%==============================================================================
\section{FEV-Bench Cross-Benchmark Validation}
\label{app:fevbench}
%==============================================================================

To verify that our findings generalize beyond GIFT-Eval, we evaluate on FEV-Bench \citep{das2024decoder} ($100$ tasks), an independent time-series forecasting benchmark, and reproduce the polynomial-degree sweep.

\begin{table}[h]
\centering
\caption{FEV-Bench polynomial-degree sweep, 5 seeds per degree. $\Delta\%$ relative to the single-seed baseline ($1.2908$).}
\small
\begin{tabular}{cccc}
\toprule
\textbf{Degree} & \textbf{MASE (mean $\pm$ std)} & \textbf{$\Delta\%$ vs.\ BL} & \textbf{SQL (mean $\pm$ std)} \\
\midrule
2 & $1.2780 \pm 0.0039$ & $-0.99\%$ & $1.0425 \pm 0.0032$ \\
3 & $1.2609 \pm 0.0173$ & $-2.32\%$ & $1.0288 \pm 0.0125$ \\
\textbf{4} & $\mathbf{1.2532 \pm 0.0064}$ & $\mathbf{-2.92\%}$ & $\mathbf{1.0236 \pm 0.0053}$ \\
5 & $1.2639 \pm 0.0069$ & $-2.08\%$ & $1.0309 \pm 0.0063$ \\
6 & $1.2521 \pm 0.0068$ & $-3.00\%$ & $1.0236 \pm 0.0060$ \\
7 & $1.2564 \pm 0.0114$ & $-2.66\%$ & $1.0263 \pm 0.0094$ \\
8 & $1.2518 \pm 0.0082$ & $-3.02\%$ & $1.0228 \pm 0.0059$ \\
\bottomrule
\end{tabular}
\end{table}

\HD{} ($d=4$) improves geometric-mean MASE by $2.91\%$ on FEV-Bench (5-seed mean $1.2532 \pm 0.0064$ vs.\ Baseline $1.2908$) and SQL (Salesforce's scaled quantile loss) by $3.09\%$ ($1.0236 \pm 0.0053$ vs.\ $1.0562$). The FEV-Bench degree sweep reproduces the qualitative GIFT-Eval pattern: every $d \geq 2$ beats Baseline, the curve is shallow, and $d \in \{4, 6, 8\}$ are statistically tied at the optimum.

%==============================================================================
\section{Comparison to Official Moirai 2.0}
\label{app:official}
%==============================================================================

Our retrained Baseline differs from the official Moirai-2.0-R-Small release due to substantially smaller training compute:

\begin{table}[h]
\centering
\caption{Comparison to official Moirai-2.0-R-Small (HuggingFace release). The gap is expected: the official model was trained with Salesforce's full compute budget.}
\small
\begin{tabular}{lcc}
\toprule
\textbf{Model} & \textbf{Geo Mean MASE} & \textbf{Training} \\
\midrule
Official Moirai-2.0-R-Small (HF) & 0.7280 & Full Salesforce budget \\
Our BL (5-seed mean, 10K) & 0.8617 & 10K steps \\
Our \HD{} (5-seed mean, 10K) & 0.8368 & 10K steps \\
Our BL (5-seed mean, 100K) & 0.8784 & 100K steps \\
Our \HD{} (5-seed mean, 100K) & 0.8443 & 100K steps \\
\bottomrule
\end{tabular}
\end{table}

The gap between our retrained models and the official release is expected and does \emph{not} affect our findings. Our experiments are designed as controlled comparisons: all conditions (Baseline, \HD{}, Zero, Duplicate) use identical training settings, data, and compute. The relative improvement from input preconditioning is the relevant finding, not the absolute MASE level.

%==============================================================================
\section{Synthetic LDS Experiment Details}
\label{app:synth-lds-details}
%==============================================================================

The synthetic linear dynamical systems are generated as follows:
\paragraph{Common setup (both experiments).}
\begin{itemize}[leftmargin=1.5em]
    \item State dimension $m = 5$, observation dimension $n = 1$, spectral radius $\rho(A) = 0.99$.
    \item Transition matrix $A \in \R^{m \times m}$ has eigenvalues $\rho \cdot \text{linspace}(0.80, 0.99, m)$ with random signs, embedded in a random orthogonal basis.
    \item Observation matrix $C \in \R^{1 \times m}$ has i.i.d.\ unit-Gaussian entries.
    \item Process noise $w_t \sim \mathcal{N}(0, \sigma_w^2 I_m)$, observation noise $\varepsilon_t \sim \mathcal{N}(0, \sigma_e^2)$, with $\sigma_w = \sigma_e = 0.02$.
\end{itemize}

\paragraph{Spectrum experiment (Figure~\ref{fig:synth-spectrum}).} We simulate one trajectory of length $T = 20{,}000$ and apply the Chebyshev preconditioner at stride $s = 3$ for $d \in \{0, 2, 4, 6\}$, then compute the Welch periodogram with $512$-point Hann window and $50\%$ overlap. The spectral attenuation at the predicted minima $\omega_k = k/(2s) = 0, 1/6, 1/3$ matches the closed-form $\abs{H(\omega_k)}^2 = 2^{2-2d}$ within Monte Carlo error.

\paragraph{Noise-gain validation (Figure~\ref{fig:synth-regret}).} We simulate $n_{\text{seeds}} = 15$ trajectories of length $T = 3{,}000$. For each, we fit AR$(L=3)$ via ordinary least squares with $\lambda = 10^{-4}$ Tikhonov regularisation on an initial prefix of length $n_{\text{train}} \in \{50, 100, 200, 400, 800, 1{,}600, 2{,}400\}$ and evaluate one-step-ahead test MSE on the remaining trajectory. Results are averaged across the 15 seeds. The full data is in \texttt{analysis/data/synth\_lds\_regret.csv}:
\begin{center}
\small
\begin{tabular}{ccccc}
\toprule
$n_{\text{train}}$ & $d=0$ MSE (no precond.) & $d=2$ MSE & $d=4$ MSE & Best \\
\midrule
$50$    & $0.00311 \pm 0.00026$ & $0.00354 \pm 0.00027$ & $0.00512 \pm 0.00039$ & $d=0$ \\
$200$   & $0.00288 \pm 0.00015$ & $0.00330 \pm 0.00013$ & $0.00475 \pm 0.00019$ & $d=0$ \\
$800$   & $0.00276 \pm 0.00008$ & $0.00318 \pm 0.00009$ & $0.00464 \pm 0.00016$ & $d=0$ \\
$2{,}400$ & $0.00273 \pm 0.00014$ & $0.00312 \pm 0.00021$ & $0.00456 \pm 0.00039$ & $d=0$ \\
\bottomrule
\end{tabular}
\end{center}

\paragraph{Comparison with Lemma~\ref{lem:noise-gain}.} Averaging across all seven $n_{\text{train}}$ values:
\begin{center}
\small
\begin{tabular}{lccc}
\toprule
& $d=0$ & $d=2$ & $d=4$ \\
\midrule
Empirical $\overline{\mathrm{MSE}}$ & $0.00286$ & $0.00328$ & $0.00474$ \\
$\overline{\mathrm{MSE}}(d{=}0) / \overline{\mathrm{MSE}}(d)$ & $1$ & $0.872$ & $0.603$ \\
Pure-noise-gain prediction $1/\norm{\boldsymbol{a}^{(d)}}_2^2$ & $1$ & $0.800$ & $0.496$ \\
Implied spectral-residual benefit & $0$ & $+0.072$ & $+0.107$ \\
\bottomrule
\end{tabular}
\end{center}
The empirical ratios sit between $1$ (no preconditioning effect) and the pure-noise-gain reference (no spectral benefit), qualitatively consistent with the spectral--noise tradeoff of Section~\ref{sec:noise-gain}: preconditioning compresses the spectral-residual term but amplifies the noise-gain term. For a forecaster well-specified for the AR structure of the data, the spectral-residual term is small to begin with, so the net effect is a higher MSE---consistent with the regime where the heuristic optimal degree (Remark~\ref{rem:noise-tradeoff}) is $d^* = 0$. The pretrained-transformer regime (Section~\ref{sec:main-results}) appears to sit on the other side of the tradeoff, where the spectral-residual term dominates and preconditioning helps; we cannot extract this purely from the regret heuristic, which is why our empirical contribution is the source of the transformer-side evidence.

%==============================================================================
\section{Spectral-Correlate Details}
\label{app:spectral-correlate-details}
%==============================================================================

For each of $39$ unique GIFT-Eval source datasets, we compute the empirical Welch power spectral density $\hat S_X(\omega)$ over the held-in training portion and a binary passband indicator
\[
    \mathbb{1}_{\text{pass}}(\omega) \;:=\; \mathbb{1}\!\big[\,\abs{H(\omega)}^2 > \mathrm{med}_{[0,1/2]}(\abs{H}^2)\,\big],
\]
where $H(\omega) = 1 - e^{-j 2 \pi \cdot 32 \omega} + (1/8) e^{-j 2 \pi \cdot 64 \omega}$ is the full-filter response (used here as a proxy for the residual response $\widetilde H = H - 1$, which has the same critical frequencies and qualitatively similar passband structure---see Section~\ref{sec:spectral-correlate}). We then form the per-dataset passband fraction
\[
    p_X \;=\; \int_0^{1/2} \mathbb{1}_{\text{pass}}(\omega) \, \hat S_X(\omega) \, d\omega \;\Big/\; \int_0^{1/2} \hat S_X(\omega) \, d\omega
\]
and regress per-dataset MASE improvement (5-seed mean, \HD{} vs.\ BL) on $p_X$ via OLS:
\[
    \widehat{\Delta\%}_X \;=\; \alpha + \beta \cdot p_X + \varepsilon_X.
\]
Results: $\beta = +2.31$ percentage points per unit passband fraction, $\hat r = +0.10$, $p_{\text{perm}} = 0.54$ ($10{,}000$ permutations). The slope is in the direction predicted by the spectral mechanism but the test does not reach significance at $\alpha = 0.05$, so we report it as descriptive evidence rather than as confirmation of the mechanism. Two refinements would strengthen the test: (i) replacing the binary indicator $\mathbb{1}_{\text{pass}}$ by the continuous magnitude $\abs{\widetilde H(\omega)}^2$ from Proposition~\ref{prop:plancherel}, and (ii) substituting the residual-filter magnitude $\abs{\widetilde H}^2$ for the full-filter magnitude $\abs{H}^2$. We leave both refinements for follow-up work.

\end{document}
